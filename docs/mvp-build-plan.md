# Вітрина — MVP Build-план (v1)

> **Мета:** живий згенерований сайт на власному домені `*.wizz-app.net`, зібраний через розмову з AI-агентом, який власник може відредагувати дотиком і опублікувати — **включно з воронкою заявок у Telegram**.
> **Скоуп:** архітектурний бриф §15 (MVP-скоуп v1, оновлено 2026-07-07). Рішення й «чому» — у брифі; тут — **порядок робіт**.
> **Стек:** Next.js (App Router, TS) на Vercel · Supabase EU (Postgres) · піддомени `*.wizz-app.net` · Sonnet 4.6.
> **Режим:** ship-fast. Кожна фаза закінчується видимою «перемогою», яку можна показати.
> **Статус (2026-07-07):** ✅ Фази 0–4 shipped.

## Легенда
- 🏁 **Перемога фази** — те, що видно на екрані наприкінці.
- ⛓️ **Залежність** — що має бути готове раніше.
- 📄 — посилання на розділ брифу.

---

## Фаза 0 — Каркас ✅ (shipped)
Ціль: порожній застосунок живе на Vercel, DNS налаштований.

1. `create-next-app` (App Router, TypeScript, Tailwind). Репо → Vercel-проєкт.
2. DNS-запис wildcard `*.wizz-app.net` → Vercel. 📄 §6 Задача 1.
3. Supabase EU; підключення (env). Zod у залежностях.
4. `middleware.ts` — кістяк розділення: платформні хости vs tenant. 📄 §2.5.

🏁 Wildcard роутинг живе; платформна заглушка і tenant-заглушка розрізняються.

---

## Фаза 1 — Модель даних + мультитенант-рендер ✅ (shipped)
Ціль: захардкоджений сайт у БД рендериться на піддомені. Це доводить хребет.

*Реальність:* міграції `tenants/pages/conversations/tenant_members/leads` + `app/s/[host]/[[...slug]]` (папки з `_` приватні в Next → перейменовано, журнал #40); `draft_content/published_content` на рівні сторінки; ISR з тег-інвалідацією; 404 для `status='suspended'` (журнал #34).

🏁 Мультитенант-рендер + ISR + kill-switch живуть.

---

## Фаза 2 — Field-схема вертикалі + генерація ✅ (shipped)
Ціль: з набору фактів AI збирає валідні блоки. Петля генерації жива.

*Реальність:* мультивертикальний реєстр (`florist/bakery/lawyer/autoservice/generic`) — vertical=data; `businessFactsSchema` generic + per-vertical override (labels, price plausibility, theme presets, advisor guidance). Генератор — tool use (structured outputs strict mode відхилив 10-варіантний union, журнал #39). `lead_form` force-injected у кожен сайт кодом (§5.6, журнал #33). Два пресети є; switchTemplate = «Перегенерувати» (журнал #36).

🏁 Факти → валідний сайт з `lead_form`. Генерація детермінована й безпечна.

---

## Фаза 3 — Агент-чат онбординг ✅ (shipped)
Ціль: нетехнічна людина в розмові доводить сайт до генерації.

*Реальність:* auth відкладено (дашборд = obscurity, журнал #34). Агент = ADVISOR + validator: класифікує вертикаль на 1-му turn, валідатори в коді (телефон/місто/ціна → issues, max 1 питання per turn), ready-gate = рішення моделі. Персистентність shipped: conversations per session, placeholder tenant з `host=null` (O1 вирішено в практиці), resume через localStorage, очищення по finalize (журнал #37). Крок підтвердження = редагована форма з фактами.

🏁 Нова людина → балакає з агентом → отримує сайт з `lead_form` на піддомені.

---

## Фаза 4 — Редагування + публікація ✅ (shipped)
Ціль: власник сам правит контент і публікує.

*Реальність:* bottom-sheet форма генерується з Zod-схеми (`lib/blocks/fields.ts`, one-registry інваріант); reorder/hide блоків; фото upload: canvas re-encode (EXIF-strip, 1600px, WebP) → `/api/upload` → Supabase Storage; theme preset switcher; «Перегенерувати» = ре-запуск генерації з фактів, старі блоки → pocket (адаптація §4.7). Draft → «Опублікувати» → purge тегів. Дашборд: список заявок (`leads`) + Telegram connect UI (deep-link token flow). 📄 §3, §4.7, §4.8, §5.5, §5.6.

🏁 Повна петля: розмова → сайт → правки дотиком → фото → публікація → заявки в Telegram.

---

## Фаза 5 — Дати друзям
Ціль: перший живий фідбек.

1. 3–5 реальних людей роблять свій сайт.
2. **Пряме спостереження** (не аналітика — §12): де завмирають у чаті? витягує агент достатньо фактів? кажуть «так, це я»?
3. Північна метрика онбордингу: чи доходять до опублікованого сайту, за скільки.

🏁 Знаєш, чи генерація-через-чат працює на живих нетехнічних людях.

---

## Поточний цикл (2026-07-08+, після валідації з партнером)
> Джерело: транскрипт обговорення 2026-07-08 + вердикти + дизайн-ревю. Бренд: **3minsite** (домени куплено; міграція з wizz-app.net — пізніше, свідомо).

0. ✅ **UI-редизайн продукту** — shipped 2026-07-08 (комmiт a51eb6d): дизайн у Claude design (бриф віддано; напрям B «Небо і мед», чат = 1b прогрес-чипи + 1c швидкі відповіді) → імплементація як дизайн-система (Tailwind-токени + спільні компоненти) по всіх поверхнях. Оркеструє Fable, екрани — Opus/Sonnet-агенти. Мобільний аудит поглинається цим кроком.
1. ✅ **Auth-гейт на генерацію** — shipped 2026-07-08 `журнал #43`: генерація ЛИШЕ для залогінених — `finalizeAction` вимагає сесію, tenant одразу отримує власника; **claim-token-флоу прибрати** (код + UI «Зареєструйтесь, щоб керувати»); чат до логіну відкритий, логін-гейт на кроці «Переглянути й створити сайт», стан розмови переживає редірект (persistence). У дизайні — екран/модал логін-гейта в цьому місці.
2. ✅ **«Відредагувати з ШІ» на рівні блоку** — shipped 2026-07-09: панель у bottom-sheet (інструкція → AI правит лише пропси блоку, Zod + grounding до фактів + 1 ретрай → результат у ФОРМУ, людина тисне «Зберегти»); rate-limit ai_edit 60/год. `quickReplies[]` + `progress[]` shipped раніше з редизайном. Далі (беклог): загальний контекстно-свідомий чат редактора.
3. ✅ **Скіни блоків** — shipped 2026-07-09: `skin` на рівні блоку (верстка, не контент); hero (класичний/split/мінімальний) + services (картки/список/компакт); генерація розігрує скін лотереєю → різні сайти виглядають по-різному; пікер «Вигляд» (чипи) у редакторі, миттєве перемикання. 🔶 **Template-mining** — конвеєр готовий (нові скіни = записи в `lib/blocks/skins.ts` + варіанти компонентів); чекає лінки вподобаних шаблонів від власників.
4. ✅ **Лендінг** — shipped 2026-07-09 (корінь платформи): герой «Клієнти з інтернету — прямо у ваш Telegram» з моком Telegram-заявки, 3 кроки, 4 фічі, чипи вертикалей, CTA → app-хост. Спрацює на 3minsite.com автоматично після міграції домену.
5. ✅ **Лічильники подій** — shipped 2026-07-09: `site_events` (view/tel_click/contact_click, без IP) + beacon на tenant-сайтах (sendBeacon) + `/api/events` (Host→tenant, fail-open); живлять місячний звіт етапу 2. ✅ **«Хочу кастомні зміни»** — shipped 2026-07-09: кнопка під прев'ю редактора → `custom_requests` + пуш у `ADMIN_TELEGRAM_CHAT_ID`; ліміт 5/добу. Міграція `0005_events_requests.sql`.
6. **Зображення: класика + runtime-генерація** `💡 2026-07-10, журнал #46 — переспековано після прототипу` — прототип nano-banana показав дрейф деталей (генеративний edit ПЕРЕМАЛЬОВУЄ сцену: флакони/відображення «поїхали») → «майже те саме фото» гарантує лише класична обробка:
   - **6а. Класичні метрики + корекція (sharp)**: аналіз при завантаженні (яскравість/розмиття/роздільність, пороги = константи в одному файлі) → мʼяке попередження з вибором (не блокує флоу) → авто-корекція (normalize/легка сатурація/різкість, нуль дрейфу) → оригінал зберігається поруч.
   - **6б. Runtime-генерація hero, коли нема фото (рішення власника: тест у MVP)**: атмосферне/нелітеральне зображення вертикалі (промпт-шаблони = ДАНІ в реєстрі вертикалей; без інтерʼєрів-як-реальних, людей, тексту — в межах #44), gemini-2.5-flash-image ~1.5 грн/сайт, hero-only, fail-open (без картинки ≠ без сайту), результат у `brand.generatedHero` → переживає «Перегенерувати» без повторної оплати; заміняється в редакторі як звичайне фото. Safeguard: власник бачить сайт до того, як поділиться лінком + підказка «перевірте зображення» на success-екрані.
   - Генеративний enhance (nano-banana) — лише опційна кнопка з превʼю, відкладено; прототип: `scripts/enhance-probe.mjs`.

## Тижневий цикл (2026-07-09) — MVP-хвіст перед банерами
> Джерело: обговорення з партнером 2026-07-09 (`partner-discussion-2026-07-09.md` §7). Мета тижня: продукт готовий до таргет-тесту.

1. ✅ **Коректні відмови агента** — shipped 2026-07-09: блок «МЕЖІ ПЛАТФОРМИ» в промпті онбордингу (чесна відмова + «Хочу кастомні зміни», без over-refusal — перевірено живими пробами); у ✨-редагуванні — опційне поле `note` в інструменті (модель пояснює неможливе, props без змін) → показується в панелі редактора.
2. ✅ **Крок «фото/лого» в чат-боті** — shipped 2026-07-09: фаза «media» між «готово» і підтвердженням (лого/вивіска + до 3 фото, «Пропустити» без блокування флоу); зберігається в `facts_state.media` на кожне завантаження → переживає логін-гейт; upload по `conversationId` (до існування host); **зображення в генерації детерміновані** — модель НІКОЛИ не бачить URL: hero=фото1, галерея від 2 фото (обрана моделлю або інжектована), grounding-прохід стрипає БУДЬ-ЯКИЙ невласний URL по всіх image-полях (§4.8/#44); лого → `brand.logoUrl` (jsonb, без міграції) → шапка сайту + кнопка «Лого» в редакторі (живе одразу, brand неверсіонований); фото переживають «Перегенерувати». E2E перевірено: HTML сайту містить лише storage-URL, нуль сторонніх.
3. ✅ **Адмінка платформи** — shipped 2026-07-09: `/admin` (fail-closed гейт `ADMIN_EMAILS`, env-only allowlist); статистика, таблиця сайтів (host≠null; placeholder-чернетки — окремим лічильником), заявки/розмови/кастом-запити, владелец по email; **kill-switch** suspend/restore з ConfirmDialog (restore → published лише якщо сторінка була опублікована). Перевірено: без сесії — false/redirect; suspend→404/restore→200 з purge кешу. ⚠️ Ліміти v1: сайти 200, заявки 5000, listUsers 1000 — з індикаторами.
3а. ✅ **Полірування чату + thinking** — shipped 2026-07-09: extended thinking на ГЕНЕРАЦІЇ (budget 6000, tool_choice auto, ~35s, `maxDuration=120` на сторінках-викликачах; у чаті свідомо БЕЗ thinking — латентність) ; рендер **жирного** в бульбашках (markdown-lite, без dep) + заборона іншої розмітки в промпті; guard «хід без питання» (детермінований ретрай — фікс бага «зберігаю й питаю далі» без питання); агентний вигляд: стадійний індикатор («Читаю → Оновлюю → Готую…») + «✓ Записав: …» з реального diff прогресу.
4. **Міграція на 3minsite.com** — DNS+Vercel+env+webhook+хости в БД (потрібен доступ власника).
5. **Десктоп-поліровка tenant-сайтів** (1440px, усі блоки × скіни).
6. **Воронка-аналітика платформи** (CTA лендінга → чат → логін → генерація → публікація) — для банерного тесту.
7. Беклог тижня: template-mining (чекає лінків), sweep осиротілих фото в Storage (загублені розмови), rate-limit на `saveMediaAction` (зараз захищений лише невгадуваним uuid + валідацією URL).

## Етап 2 (після MVP) — ріст і монетизація
> Ризик #32 закрито: воронка shipped разом з MVP.

1. **Білінг (UAH)** `💡 2026-07-08, журнал #42`: **WayForPay основний** (регулярні платежі тижд/міс/рік, статуси, підписні сторінки, API) / **LiqPay альтернатива** (Підписка API + нативна розстрочка «Оплата частинами» 2–25). **Stripe відхилено для UA-старту** (нема UA-онбордингу; закордонна юрособа = КІК/бухгалтерія/FX; без UA-розстрочки) — другий рейл для міжнародки через **`BillingProvider`-абстракцію** в коді. Власна state-machine `активний→прострочка→грейс→призупинено` (~3–5 днів; kill-switch уже є). **Код — після ручного тесту готовності платити.** Тарифна механіка на вирішення тоді ж: teaser-заявки («телефон зірочками» понад ліміт) vs чесний ліміт із лічильником — обидві в беклозі.
2. **GBP-інтеграція** (§10.3) — NAP-консистентність, посилання сайту у GBP-лістингу; заявку на GBP API подати рано (розгляд тижнями).
3. **Звіт у Telegram = двигун ретеншну І апселів** — «X переглядів, Y заявок» + AI-рекомендації **з лінком назад у бота** (апсели живуть усередині звіту: мультисторінки, кастом). Лічильники подій почати писати раніше.
4. **Booking-request** (бажана година/дата як опційне поле lead_form) — конфіг полів на вертикаль = дані в реєстрі.
5. **Платні канали заявок**: Viber-пуш і SMS (TurboSMS) — add-on'и (реальна собівартість за повідомлення); Telegram лишається безкоштовним (собівартість ~0).
6. **«Оновлюй мене сам»** — місячний пінг від бота: власник кидає фото/новини → AI оновлює draft.
7. **Кастом-замовлення** — кнопка «Хочу кастомні зміни» → заявка нам → рахунок (~$300); concierge-клапан як апсел + генератор лідів.
8. **Мультисторінки** (сторінка на послугу — модель даних готова, §5.1.1) — апсел, тригериться рекомендацією у звіті.
9. **Фото/лого з вивіски** — «сфотографуй вивіску» як шлях лого (аудиторія не вантажить SVG); image-AI (~4 грн/фото) — експеримент із бюджетом, межі §4.8.
10. Власні домени (Задача 2), реєстрація .com.ua (Задача 3) — при платному попиті; націнка $2–3 — потім, на пролонгації.
11. **«Запуск реклами в один клік» (пілот руками)** `💡 2026-07-09, журнал #45`: схема — **наш MCC → сабакаунт на клієнта → наш платіжний профіль → передоплата пакетом** (клієнт не бачить Google-кабінету; НЕ спільний акаунт — сабакаунти ізолюють бан-радіус). Перевага: конверсії з наших `site_events` без зусиль клієнта. Межі: **нуль коду** — пілот руками на 3–5 клієнтах search-intent вертикалей (СТО/юрист/сантехнік), фіксований пакет із прозорим розбиттям бюджет/запуск, «гарантуємо запуск і покази, не заявки»; податковий транзит (~5% ФОП + 20% ПДВ Google) — у ціну. Послідовність: **після GBP (п.2)** — спершу безкоштовна Maps-видимість, реклама як апсел. Автоматизація через Ads API — лише після успішного пілота. Перед пілотом: перевірити CPC у Keyword Planner (2–3 вертикалі × 2–3 міста).

---

## Статус відкритих питань (оновлено 2026-07-07)
- ✅ **Фази 0–4 shipped.** Повна петля: онбординг → генерація → редагування → фото → публікація → заявки в Telegram.
- ✅ **Версія Next зафіксована:** 15.5.20.
- ✅ **Модель даних:** `docs/data-model.md` + міграція `0001_init.sql` + `leads` таблиця. Закрито §5.5.
- ✅ **switchTemplate shipped** як «Перегенерувати» (журнал #36).
- ✅ **Structured outputs:** tool use підтверджений (strict mode відхилив union, журнал #39).
- ✅ **O1 вирішено в практиці:** placeholder tenant з `host=null` на старті чату (журнал #37).
- ✅ **Воронка заявок у MVP** (журнал #33, §5.6).
- ✅ **Auth** — shipped (2026-07-08, §3.1): email+пароль (Supabase Auth, `@supabase/ssr`), гейт на «Мої сайти»/«Заявки»/редактор через route group `(protected)`; ownership: `requireMember` в усіх server actions редактора + списки scoped по `tenant_members`. Вмикається наявністю `NEXT_PUBLIC_SUPABASE_ANON_KEY`. **Оновлено рішенням власника 2026-07-08 (журнал #43): генерація ЛИШЕ для залогінених** — анонімна генерація і claim-token-флоу скасовані (чат до логіну відкритий, гейт на «Створити сайт»); імплементація — поточний цикл, п.1. Phone+SMS — наступний крок, як і планувалось.
- 💡 **Білінг-провайдер** — вердикт-схильність зафіксовано (журнал #42): WayForPay основний / LiqPay альтернатива, Stripe — не для UA-старту; фінальна фіксація при білінг-прототипі, код після ручного тесту готовності платити.
- ✅ **O2 rate-limit** — shipped (2026-07-08): fixed-window лічильники per-IP у Postgres (`rl_bump`, міграція 0003) на 5 поверхнях: створення розмови (30/добу), chat turn (240/год + cap довжини розмови), генерація (15/добу), заявки (15/хв), upload (120/год). Пороги свідомо м'які (~10× від чесного користувача, CGNAT-safe) і крутяться через env `RATE_LIMIT_*` без коду; `RATE_LIMIT_DISABLED=1` — глобальний вимикач. Fail-open: збій лічильника ніколи не блокує користувача. Kill-switch = `status='suspended'` лишається другим шаром.
