# Вітрина — Архітектурний бриф і журнал рішень

> **Призначення документа:** контекст і зафіксовані рішення для AI-агента (Claude Code), який будуватиме продукт.
> Документ описує *що вирішено*, *що ще відкрите* і *чому* — щоб агент не перевигадував уже узгоджене і не робив наївних припущень.
> **Статус:** концепт-фаза, до написання коду. Це не фінальна специфікація, а зафіксований стан мислення.
> **Версія:** v2.2 (2026-07-06) — продуктові рішення власника + звірка зі старим бізнес-планом: цільовий клієнт (1), бізнес-модель self-serve явно зафіксована (1.1), **воронка заявок → Telegram як ЯДРО цінності (5.6)**, редагування «натиснув → панель знизу» (3), Viber і аналітика — не в MVP. Дельти #23–#28 у розділі 14.
> v2.1 (2026-07-02) — додано рішення за підсумком fresh-eyes ревю: платформний роутинг (2.5), auth власника (3.1), draft/publish (5.5), алгоритм switchTemplate (4.7); виправлено суперечність matcher/301 та кеш-подію зміни canonicalHostname. Дельти #14–#22 у розділі 14.
> v2 (2026-06-30) — інтегровано правки за підсумком технічної валідації трьох підсистем (мультисторінковість, per-tenant SEO, AI-композиція секцій). Дельти #1–#13 у розділі 14 «Журнал рішень».

## Як читати цей документ (легенда статусів)

- ✅ **ВИРІШЕНО** — рішення прийняте, не перевигадувати без явного запиту.
- 🔶 **ВІДКРИТЕ** — потребує рішення людини або уточнення перед реалізацією.
- 💡 **РЕКОМЕНДАЦІЯ** — запропоновано, але людиною ще не підтверджено.
- ⚠️ **РИЗИК / ОБМЕЖЕННЯ** — усвідомлений ризик, який треба тримати в полі зору.
- **ІНВАРІАНТ** — наскрізне обмеження, яке не порушувати в реалізації (hard-constraint для агента).
- `🔶→✅ (дата)` — слід зміни статусу: питання було відкрите, закрите валідацією. Під таким рішенням рядок `Відкинуто:` зберігає, який варіант і чому відкинуто.

---

## 1. Контекст продукту

**Що це:** AI-конструктор сайтів. Користувач вводить деталі про свій бізнес → система генерує готовий сайт із доменом → і **кожна заявка з сайту падає власнику в Telegram**.

**Цільовий клієнт (платник) `🔶→✅ (2026-07-06)`:** **нетехнічний власник малого бізнесу будь-якого віку** (квіткарні, барбершопи, юристи, локальні послуги), готовий трохи поколупатись сам. **50+ — це лінза дизайну, а не віковий гейт:** проєктуємо під найбоязкішого/найменш впевненого юзера (50+ — його найгостріший приклад) — curb-cut ефект: «коли просто для них — просто для всіх». Молодший родич, що налаштовує сайт за батька, — це розширення аудиторії, не провал.

> *Відкинуто:* «аудиторія = лише 50+». Замикає продукт і маркетинг на пенсіонерах; стара аналітика попереджала, що саме 50+ найгірше конвертують у self-serve. Ширший таргет (нетехнічні будь-якого віку) робить обрану self-serve-модель (1.1) когерентною. Журнал #23.

**Ключова теза (уточнена `🔶→✅ (2026-07-06)`):** генерація сайту — це ~20% продукту і ~100% демо; ринок її вже вирішив (Durable, Weblium, Wix) — це **table-stakes**. Реальне **ядро цінності — воронка заявок: форма/кнопки на сайті → заявка в месенджер власника** (§5.6), туди, де він і так сидить. «Просто мати сайт» власнику нічого не дає; цінність — коли з нього приходить клієнт. Решта складності — в інфраструктурі: редагування, домени, мультитенантний хостинг, білінг у гривні, підтримка, економіка.

> *Відкинуто:* ставити AI-генерацію в центр пітчу. Конкурентна звірка: генерація <60с — комодіті; єдина реально відсутня в UA фіча — пуш заявки власнику. UAH-білінг і месенджер-кнопки-віджети теж уже є в конкурентів (HostPro/FREEhost) → гігієна, не диференціатор. Журнал #24.

### 1.1. ✅ Бізнес-модель — self-serve «зроби сам» `🔶→✅ (2026-07-06)`
- **Обрано self-serve:** робот генерує сайт, власник **сам** редагує (простими дотиками, §3), платить **гривневу підписку**. НЕ concierge («жива людина зробить за вас») — оператор у MVP не потрібен.
- ⚠️ **Свідомо прийнятий ризик:** стара бізнес-аналітика рекомендувала concierge й попереджала, що частина аудиторії може не конвертувати в self-serve (боїться, хоче «щоб зробили за неї»). Приймаємо цей ризик і перевіряємо його **швидким шипом**, а не наперед; обраний ширший таргет (1) цей ризик знижує.
- **Режим роботи — startup: ship fast → дати живим людям → міняти швидко по фідбеку.** На ранній стадії фідбек = **пряме спостереження за друзями-тестувальниками** (краще за аналітику, поки можна дивитись через плече). Формальна аналітика — не в MVP (§12); тригер увімкнення — вихід на реальних чужих користувачів.
- Не плутати з §2.3 «Модель A/B»: там ідеться про модель **рендеру**, а не про бізнес-модель.

> *Відкинуто:* concierge-hybrid (оператор будує за клієнта + build-fee). Суперечить бажанню «хай роблять самі» й додає операційне вузьке місце; лишається запасним варіантом, якщо швидкий шип покаже, що self-serve не конвертує. Журнал #25.

---

## 2. ✅ Архітектурний хребет (наскрізний принцип)

П'ять рішень, від яких залежить усе інше. Вони мають дотримуватись скрізь.

### 2.1. Контент = дані, а не HTML
Сайт користувача зберігається як **структуровані дані** (поля, списки, налаштування), а не як згенерований HTML/CSS.

**Наслідки, які тримати в голові:**
- Дозволяє редагувати контент через прості поля, не чіпаючи верстку.
- Дозволяє автоматично будувати SEO-розмітку (structured data) з тих самих полів.
- Дозволяє перегенерувати дизайн / поміняти шаблон, не торкаючись контенту. (Технічно це забезпечується тим, що **дизайн зберігається окремо від контенту як design tokens** — див. 4.5.)
- Дозволяє годувати ті самі дані в інші канали (сповіщення тощо) пізніше.

### 2.2. Один застосунок рендерить усі сайти (мультитенантність)
Ми **НЕ** хостимо N окремих сайтів. Ми хостимо **один** застосунок, який рендерить усі сайти з даних.

- «Сайт клієнта» — це **НЕ папка з кодом і НЕ окремий деплой. Це рядок у базі.** 5000 клієнтів = 5000 рядків у Supabase, а не 5000 застосунків. Код однаковий, відрізняються лише **дані**. Аналогія: користувачі в застосунку (один білд, дані юзера визначають, що показати) — тільки тут «хто це» визначається не логіном, а **доменом**.
- Запит → читаємо `Host`-заголовок → визначаємо tenant → тягнемо контент цього клієнта з БД → рендеримо. Той самий код обслуговує всіх.
- Структура роутів у Next: **немає файлів-під-клієнта**. Один catch-all роут (`app/_sites/[host]/[[...slug]]/page.tsx`) + `middleware.ts` (Host → rewrite у внутрішній шлях `/_sites/{host}/...`) + `lib/getTenant` + `lib/getPage`. Додати 5000-го клієнта → **0 нових файлів**.
- Деплоїмо **раз**. Фікс бага = push один раз → усі сайти оновлюються миттєво (це фізично один застосунок).

### 2.3. ✅ Data-driven рендер, а НЕ pre-generated файли
Є дві принципово різні моделі. Зафіксовано **Модель A**. Агент не повинен дрейфувати в бік B.

- **Модель A (обрано):** один data-driven застосунок рендерить кожен сайт із БД на льоту і **кешує** результат. Сайт = дані.
- **Модель B (відкинуто):** генерувати реальні статичні HTML-файли під кожного клієнта і складати на CDN. Сайт = набір файлів.
- **Чому A:** глобальний фікс/редизайн у B = перегенерувати всі сайти; складніша динаміка, інвалідація й сховище. ISR/кеш у моделі A дає швидкість статики (плюс B) **без** його мінусів — рендер один раз, віддача з кешу як статика, джерело — один застосунок + дані.

### 2.4. ✅ Tenant визначається на запиті через канонічний hostname `🔶→✅ (2026-06-30)`
Наскрізний інваріант, доданий за підсумком валідації роутингу й SEO. Він одночасно тримає правильний роутинг, коректне per-tenant SEO без втрати кешу і чисту прив'язку нав/секцій до даних.

- **ІНВАРІАНТ:** у моделі tenant є поле **`canonicalHostname`** — один обраний host клієнта (піддомен або власний домен). Усі похідні URL (canonical, `og:url`, посилання в sitemap, `url`/`image` у JSON-LD, `metadataBase`) будуються **з `canonicalHostname`**, а **не** з host поточного запиту і **не** з глобального домену платформи.
- **ІНВАРІАНТ:** host у рендер-шляху береться з **rewrite-`params`** (сегмент `[host]` після middleware-rewrite), а **не** з `headers()`. `headers()` переводить сторінку в динамічний рендер і вбиває ISR/кеш. Виняток — `app/sitemap.ts` / `app/robots.ts`, де `params` недоступні: там легітимно `headers().get('host')` (це навмисно робить їх динамічними per-host; запити в БД кешувати).

> *Відкинуто:* брати host з `headers()` усюди задля простоти — ламає кешування на тисячах сайтів. Журнал #1.

### 2.5. ✅ Платформа vs тенанти — розділення роутингу `🔶→✅ (2026-07-02)`
Один застосунок обслуговує ДВА різні світи, і middleware мусить розрізняти їх першим кроком:

- ✅ **Дашборд/редактор живе на `app.vitryna.com.ua`.** Кореневий `vitryna.com.ua` — маркетинг-лендінг платформи. Сайти клієнтів — усі інші hosts (піддомени + власні домени).
- **Порядок у middleware:** (1) host у списку платформних (`vitryna.com.ua`, `www.`, `app.`) → платформний роутинг, БЕЗ tenant-rewrite; (2) інакше → tenant-резолв за host; (3) **невідомий host** (не платформа, не tenant) → 404-заглушка платформи, не спроба рендеру.
- **ІНВАРІАНТ:** middleware **НЕ ходить у Postgres на кожен запит** (це і є заборонений «compute за перегляд», §9). Мапа host→tenant — з edge-кешу (KV / Edge Config / in-memory TTL), інвалідується при зміні доменів tenant (див. кеш-подію в 10.2).
- **Уточнення двозначності 5.2:** rewrite завжди робиться на **`canonicalHostname`** (після 301 request-host і так дорівнює канонічному), тобто кеш одного tenant існує **лише під одним** host-шляхом — без подвійного кешування.

> *Відкинуто:* дашборд на кореневому домені (`vitryna.com.ua/admin`) — конфліктує з маркетинг-сайтом і змішує кеш платформи з маркетингом. Журнал #14.

---

## 3. ✅ Редагування контенту (модель UX)

**ВИРІШЕНО: редагування ПОЛЯМИ, поданими як «натиснув на елемент → знизу висунулась панель з полем», а не таблиця полів (CMS-стиль) і не канвас.** `🔶→✅ (2026-07-06)`

- **Взаємодія:** власник дивиться на свій реальний сайт → **натискає на текст / фото / пункт списку** → знизу висувається **панель (bottom sheet) з підписаним полем** саме цього елемента + велика кнопка «Зберегти». Відчувається прямо («я торкнувся того, що хотів»), але лишається безпечним.
- **ІНВАРІАНТ (запобіжник під нетехнічну аудиторію):** натиск відкриває **сфокусований редактор ТІЛЬКИ цього елемента**. **Ніякого перетягування, зміни розміру, руху секцій** — усе, що дозволяє «зламати макет», заборонено. Це не канвас: під капотом ті самі поля схеми блоку (4.1), просто гарніший вхід у них.
- **Чому не таблиця полів:** «CMS з полями» холодна й незрозуміла для аудиторії. **Чому панель знизу, а не редагування прямо в тексті:** на телефоні клавіатура закриває пів екрана, редагувати «в тексті» непевній людині незручно — панель з однією зрозумілою кнопкою спокійніша.

Причина рішення «поля, не канвас» — не лише дешевизна розробки: для цієї аудиторії полотно, яке можна «зламати», лякає; сфокусоване поле — ні.

> *Відкинуто:* (а) канвас/drag-and-drop — лякає, легко зламати; (б) таблиця полів окремо від сайту — холодна, незрозуміла; (в) inline-редагування прямо в тексті — незручно на телефоні. Журнал #26.

**ІНВАРІАНТ (зв'язок зі схемою блоків):** оскільки кожен блок має схему слотів (див. 4.1), **форма редагування генерується зі схеми блоку автоматично** (підхід Puck: `fields` → UI-форма → серіалізація назад у `{type, props}`). Один реєстр блоків обслуговує і рендер, і валідацію, і опис для AI, і форму редагування — їх не можна розсинхронізувати.

**Мінімальний набір редагування, що покриває ~90% потреб без канваса:**
1. Повторювані елементи списку — додати/прибрати послугу, товар, відгук, члена команди.
2. Сховати/показати цілу секцію.
3. Переставити елементи всередині списку.
4. Замінити зображення.
5. ✅ **«Перемкнути шаблон» `🔶→✅ (2026-06-30)`** — обрати інший кураторський пресет; контент-факти переносяться у слоти нового каркасу по реєстру блоків (нерозпізнане лишається в чернетці). Це **клапан тиску** під ризик первинної генерації (нижче) — без нього невдалий перший вибір замикає нетехнічного користувача й гробить онбординг. Перенесено з post-MVP у MVP. Це **перемикання пресету, а не редагування макета** — канвасом не стає.

> *Відкинуто:* тримати «перемкнути шаблон» у post-MVP як «дорогу операцію». Валідація показала, що без нього структурну помилку генерації користувач не виправить полями — пряма причина відмов. Журнал #5.

Усе, що далі (переставляти **секції** вручну, міняти кольори/макет руками), вже тягне справжній редактор → поза скоупом MVP.

⚠️ **Плата за це рішення:** планка якості *первинної* генерації зростає. Якщо людина править лише значення полів, то структуру/макет/дизайн AI мусить вгадати з першого разу — структурну помилку користувач не виправить **полями**. Саме тому «перемкнути шаблон» (п.5) втягнуто в MVP як страховку.

🔶 **ВІДКРИТЕ:** field-схеми під кожен тип бізнесу (вертикаль) ще не спроєктовані. Це і є прихована вартість продукту — не редактор, а бібліотека шаблонів + їхніх контент-моделей. Універсальна схема буде або занадто жорсткою, або занадто прісною → потрібні окремі схеми на вертикаль.

### 3.1. ✅ Вхід власника (auth) `🔶→✅ (2026-07-02)`
- ✅ **Фазово:** MVP — **email + пароль** (Supabase Auth з коробки, дешево); цільовий стан — **телефон + SMS-код** як основний метод для найменш впевненого користувача (50+ — його приклад; телефон завжди під рукою і вже є в даних бізнесу). Модель закладати так, щоб додавання phone-auth було конфігурацією, а не міграцією (Supabase підтримує обидва).
- ⚠️ «Забув пароль» для нетехнічних 50+ = потік тікетів у підтримку (§11) — тому email+пароль свідомо тимчасовий компроміс, перехід на телефон+SMS не відкладати надовго.
- ✅ **Зв'язок user↔tenant — членством одразу:** `tenant_members(user_id, tenant_id, role)`, а не жорсткий `owner_id`. Для цієї аудиторії «донька/помічник редагує сайт» — реалістичний сценарій; членство зараз дешеве, дороге потім.
- **RLS-політики** на `pages`/`blocks`/`tenants` — через членство (площина редагування/дашборду, див. уточнення в §9). Публічний рендер RLS не використовує — читає published-контент сервісним ключем через кеш.

> *Відкинуто:* жорсткий `tenants.owner_id` (один власник назавжди) — членство зараз дешеве, дороге потім; «донька редагує сайт» — реальний сценарій аудиторії. Журнал #15.

---

## 4. Генерація сайту

✅ **Підхід:** система блоків — AI обирає компоновку з **кураторських шаблонів** і заповнює слоти контентом + підбирає палітру (модель Durable / Wix ADI / Framer AI). **НЕ** вільна генерація сирого HTML/CSS (нестабільно, криво).

### 4.1. ✅ Реєстр блоків — єдине джерело істини `🔶→✅ (2026-06-30)`
Технічна основа генерації, додана за підсумком валідації.

- **Блок (секція)** = пара: **React-компонент** + **схема його слотів/полів** (напр. Zod). Сторінка = впорядкований **масив інстансів блоків** з даними: `[{type:'hero', props:{...}}, {type:'services', props:{items:[...]}}, {type:'lead_form', props:{...}}, ...]` (патерн Strapi Dynamic Zones / Puck / Sanity Portable Text). `lead_form` — особливий: несе серверний хендлер сабміту (5.6).
- **Реєстр** мапить `blockType → компонент` і `blockType → схему`. Рендер = прохід масивом + валідація `PageSchema.parse()` **до** рендеру (невідповідність ловиться як помилка, не падає в рантаймі). `UnknownBlock` як fallback, щоб сайт ніколи не падав.
- **ІНВАРІАНТ:** усю генерацію блоків валідувати проти реєстру. Блок або поле поза реєстром — **помилка, а не «творча вільність»**. Той самий реєстр обслуговує рендер + валідацію + опис для AI + форму редагування (3) + нав-якорі (5.3).

> *Відкинуто:* вільну генерацію розмітки без реєстру — давала неіснуючі блоки/поля (галюцинації структури) і робила `switchTemplate` недетермінованим. Журнал #6.

### 4.2. ✅ AI робить рівно два рішення (пресет + слоти) `🔶→✅ (2026-06-30)`
Для надійності першої генерації AI **не збирає сайт із нуля вільною композицією**, а:
1. **Класифікує вертикаль** → обирає **кураторський пресет-каркас** (набір + порядок блоків + тема під вертикаль; напр. `florist`: `[hero, services, gallery, testimonials, contacts, lead_form]`). `type`/вертикаль — `enum` із замкнутого списку. **ІНВАРІАНТ (повний продукт):** блок воронки заявок (`lead_form` + `contacts` з кнопками месенджерів, §5.6) присутній у **КОЖНОМУ** пресеті — це ядро цінності, не опція. ⚠️ **У MVP-скоупі v1 (§15) `lead_form` відкладено** — рендериться лише текстовий `contacts`; воронка = етап 2.
2. **Заповнює слоти** фактами користувача.

Пресет може позначати частину блоків **опціональними** (додаються/прибираються в межах дозволеного списку — напр. `testimonials`, якщо відгуків нема). Це не вільна композиція: каркас і порядок лишаються кураторськими.

> *Відкинуто (на MVP):* вільну композицію (AI сам обирає й сортує блоки з нуля) — ширший простір помилки, нижча надійність першої генерації. Лишається на «розширений режим» пізніше. Журнал #6.

### 4.3. ✅ Структурований вивід, а не сирий текст `🔶→✅ (2026-06-30)`
- **ІНВАРІАНТ:** AI повертає **валідований структурований JSON**, що відповідає схемі сторінки, а не HTML. Надійний базовий механізм — **tool use з `input_schema`** (модель повертає аргументи інструмента `build_page`); `type` як `enum` + `additionalProperties:false` фізично не дають вигадати неіснуючий блок чи поле. Опис доступних блоків для AI генерувати з тих самих схем (`zod-to-json-schema`), щоб опис і валідатор не розходились.
- Запобіжник: після відповіді `PageSchema.parse()`; помилка → повернути моделі текст помилки на 1–2 ретраї.

⚠️ **Звірити перед інтеграцією (статус міг змінитися):** Anthropic **structured outputs** — сильніша надбудова (constrained decoding). На момент написання — beta; статус (GA/beta) і точний синтаксис (`output_config.format` vs старий `output_format` + бета-хедер) звіряти в актуальних docs Anthropic, **не кодити з пам'яті/туторіалів**. Базовий, гарантовано доступний шлях — tool use з `input_schema`.

### 4.4. ✅ Контроль галюцинацій КОНТЕНТУ (grounding) `🔶→✅ (2026-06-30)`
Розділяємо два різні рівні — і це критично для бізнесу:
- **Структура** (вибір/порядок блоків, палітра) — креатив AI, галюцинація прийнятна.
- **Факти** (послуги, ціни, телефон, адреса, відгуки) — **тільки з анкети користувача**. Єдине дозволене джерело фактів — структурована форма вводу.
- **ІНВАРІАНТ:** маркетинговий «обгортковий» текст (хедлайни, заклики) AI писати може; реквізити/ціни/контакти — копіюються 1:1 і **пост-валідуються прямим порівнянням рядків**, а не довірою моделі. Структуру обирати окремим кроком від мапінгу фактів.
- **Механіка розрізнення:** кожне поле у схемах реєстру (4.1) анотується **`fact` | `creative`** — пост-валідація порівнює лише `fact`-поля. Перед порівнянням — **нормалізація формату** (телефон `067…` ↔ `+38 067…`, пробіли/регістр в адресі), щоб переформатування моделлю не читалось як вигадка.
- ✅ **Механізм збору фактів — агент-чат (4.9), не форма-анкета.** 🔶 Лишається спроєктувати: **field-схема першої вертикалі** (обов'язкові поля = критерій «достатньо інфи» для агента), генерація піддомену з української назви (транслітерація + перевірка колізій), момент створення tenant.

> ⚠️ Structured output гарантує лише *форму*, не *правду*: модель може ідеально валідно вписати вигадану ціну. Тому grounding (4.4) обов'язковий і незалежний від валідації схеми (4.3).

### 4.5. ✅ Тема/палітра/типографіка як design tokens `🔶→✅ (2026-06-30)`
- **ІНВАРІАНТ:** дизайн зберігається **окремо від контенту** як набір токенів (`theme: {colors, fonts, radius, spacing}`). Блоки **не містять** кольорів/шрифтів — читають токени через CSS-перемінні / ThemeProvider. «Перегенерувати дизайн, не торкаючись контенту» (2.1) = замінити об'єкт `theme`, масив `blocks` недоторканий. AI для дизайну повертає **тільки** об'єкт `theme` за схемою з обмеженими значеннями.

### 4.6. ⚠️ Усвідомлені ризики генерації
- **Галюцинації структури** — закрито реєстром (4.1) + структурованим виводом (4.3).
- **Галюцинації контенту** — закрито grounding (4.4).
- **Зображення:** сток виглядає генерично; AI-генерація дорога й не відповідає реальному товару (квіткарка хоче *свої* букети); змусити аудиторію 50+ завантажити нормальні фото — окремий бій.
- **Версіонування схем блоків:** старі сайти збережені під старою схемою. Поле `schemaVersion` у блоці + міграції (поле спершу `optional`, потім міграція; старі компоненти тримати, поки не мігровано всі сайти).
- ⚠️ **scaled-content-abuse:** той самий пайплайн дозволяє масово штампувати тонкі/дублікатні сторінки — ризик SEO-спаму й репутації кореня. Детально й мітигації — у 10.4.

✅ **Рішення — див. 4.8** (реальні фото + обробка на клієнті; AI лише трансформує реальне, ніколи не фабрикує товар).

### 4.7. ✅ Алгоритм «перемкнути шаблон» (switchTemplate) *(механізм додано 2026-07-02; саме рішення ✅ з §3 п.5)*
Механізм фічі з §3 п.5 (щоб «✅» не був декларацією без алгоритму):
1. Новий пресет застосовує **свій каркас** (набір+порядок блоків) і **свою тему-токени**.
2. Контент мапиться **за `blockType` через реєстр** (4.1): блок є в обох каркасах → `props` переносяться (схема одна й та сама, бо реєстр спільний).
3. Блоки, яких **нема** в новому каркасі → у «кишеню» чернетки сторінки (**не видаляються**): повертаються при зворотному перемиканні або додаються вручну, якщо пресет дозволяє опціональні блоки (4.2).
4. **Факти не губляться незалежно від блоків** — вони живуть у tenant-рівні анкети (4.4), блоки лише проєкція.
5. Тема: MVP-правило — **тема завжди від нового пресету** (палітру можна перегенерувати окремо, 4.5); не намагатися «зберегти стару палітру в новому каркасі» — це комбінаторика, якої MVP не потребує.
6. Уся операція відбувається у **draft** (5.5): власник бачить прев'ю, «Опублікувати» застосовує.

### 4.8. ✅ Зображення — стратегія (MVP-мінімум, не блокер) `🔶→✅ (2026-07-06)`
Принцип одним рядком: **трансформувати реальне — ніколи не фабрикувати неіснуючий товар.** Межа не «на основі фото користувача чи ні», а «чи існує фізично те, що бачить клієнт».

**MVP (мінімум, щоб продукт не був фейковим):**
- Завантаження реальних фото з галереї телефона (мобільний онбординг, multi-select, велика кнопка).
- **ІНВАРІАНТ обробки на клієнті ДО R2:** конвертація HEIC, фікс EXIF-orientation, **стрип EXIF-GPS** (приватність — фото з дому містить координати), стиснення. Інакше iPhone-фото б'ються, а оригінали роздувають R2 і швидкість.
- resize у 2–3 розміри + WebP + srcset + lazy-load (сайти image-heavy; користувач на 3G).
- **Точка фокуса** в моделі зображення (вертикальні телефонні фото в горизонтальний hero → без focal-point ріже голови).
- Кураторський безкоштовний сток (~50, Unsplash/Pexels) лише для **hero-фону по нішах** + **явні заглушки** в галереї («Натисніть, щоб додати своє фото»); публікацію м'яко блокувати, поки заглушки не замінені.
- **Модерація завантажень (NSFW/CSAM)** — обов'язок хостера чужих зображень, інакше ризик бану бакета/доменів у Cloudflare.
- Простий auto-enhance без ML: smart-crop + автояскравість/контраст.

**v1.1+:**
- **AI покращує/приводить до одного стилю РЕАЛЬНІ фото** (кілька фото → «одна професійна зйомка», чистка/заміна фону, outpaint під формат слота). Це і є легітимна форма «генерації на основі кількох фото користувача».
- **Telegram-бот для прийому фото** («перешли боту фото робіт → з'являться на сайті») — недооцінений UA-важіль; лягає на той самий центральний бот (5.6).
- Атмосферний AI hero-фон під бренд (кольори/настрій, **не товар**).

**⛔ Ніколи:** генерувати **товарні/галерейні** зображення (речі, які клієнт може замовити), навіть «у стилі» користувача на основі його фото — товар фізично не існує → негативна заявка + скандал у місцевій громаді + churn. Найпереконливіший фейк — найнебезпечніший.

**Метрика онбордингу:** `time-to-first-real-photo` (ціль <60с) — найнебезпечніший момент воронки між «згенерований сайт із заглушками» і «сайт з моїми фото».

> *Відкинуто:* (а) сток/AI-ген як основа товару — обман, б'є по довірі й по ядру (заявки); (б) обов'язкове завантаження як бар'єр — вбиває конверсію; (в) per-tenant fine-tune/LoRA для генерації товару — дорого/повільно × тисячі тенантів, і не потрібне: one-page сайту треба ~5–8 реальних фото, а не «розмноження». Журнал #29.

### 4.9. ✅ Онбординг — агент-чат, не форма-анкета `🔶→✅ (2026-07-06)`
Факти для генерації збирає **розмовний AI-агент**, а не багатокрокова форма.

- **ІНВАРІАНТ:** чат — **лише інтерфейс**; джерело істини — **структурований об'єкт фактів за field-схемою вертикалі** (§4.4). Агент заповнює схему з діалогу; генерація йде з **підтверджених** фактів, НЕ з транскрипту.
- **«Достатньо інфи» = обов'язкові поля схеми заповнені** (не «на відчуття») → **field-схема вертикалі — критичний шлях, будувати ПЕРШОЮ.**
- **Крок підтвердження = редагована форма, префілена агентом** (не стіна тексту, яку нетехнічний юзер проскролить і тисне «так»). Схлопує «підтвердження» і «форму» в один артефакт — тобто форма існує в продукті так чи так.
- **Питання батчами, не по одному × 15** (інакше повільніше й нудніше за викинуту анкету): тривіальне разом («назва, місто, телефон — одним повідомленням»).
- Перезапис полів (last-wins при «ой, телефон інший»); ескейп-хетч «просто згенеруй» → **керована деградація** (заглушки/опустити блоки, як §4.8), не ворота галюцинацій; **персистентність** `conversations(tenant_id, site_id, messages[])` з 1-го дня (мобільний закриє вкладку на 7-му питанні); **набір еталонних діалогів** для тесту (розмовний флоу має комбінаторний простір відмов — інакше ітеруєш промпти наосліп).
- Контент-**правки** лишаються дотиком-панеллю (§3); агент — для збору/генерації, не для редагування розмовою.

> *Відкинуто:* багатокрокова форма-анкета як ЄДИНИЙ вхід — агент тепліший для аудиторії й demo-wow. Але оскільки джерело істини = схема, а підтвердження = форма, форма все одно живе в продукті (тобто спір «форма vs агент» частково фіктивний). Журнал #30.

---

## 5. ✅ Структура клієнтського сайту

«Сайт» — це не одна сторінка. Дані живуть на **двох рівнях**, і це дві різні сутності.

### 5.1. Дворівнева модель даних
- **Рівень сайту (tenant config):** бренд/лого, назва бізнесу, пункти навігації, футер (контакти, години, соцмережі, копірайт), вибраний шаблон + палітра (як design tokens, 4.5), **`canonicalHostname`** (2.4), `nav_mode`, **анкета-факти** (структуроване джерело для генерації/grounding, 4.4 — саме тому факти не губляться при switchTemplate, 4.7), **канали контакту власника + `telegram_chat_id` для пуша заявок (5.6)**. Один запис на клієнта, **спільне для всіх сторінок**. Рендериться оболонкою (layout), що обгортає будь-яку сторінку tenant.
- **Рівень сторінки:** список блоків/секцій конкретної сторінки.

Хедер/футер/нав НЕ копіюються в кожну сторінку — вони з рівня сайту.

### 5.1.1. ✅ «Сторінка» — завжди сутність, навіть для one-page `🔶→✅ (2026-06-30)`
- **ІНВАРІАНТ моделі даних:** у схемі **завжди** є сутність `pages`, навіть для односторінкового MVP — там просто **одна** сторінка зі `slug = ''`. Саме це робить перехід one-page → multi-page безболісним: не міняємо модель, а додаємо рядки в `pages`.

Орієнтовна форма (закріплено як еталон, деталі типів — на етапі дизайну БД):
```
tenants(id, host, canonical_hostname, nav_mode 'onepage'|'multipage', brand/футер..., ...)
pages(id, tenant_id, slug, page_type, title, is_published, show_in_nav, nav_order,
      UNIQUE(tenant_id, slug))            -- '' = home; захист від колізій slug
                                          -- draft/published: форма версіонування контенту — 5.5
blocks(id, page_id, block_type, position, anchor, show_in_nav, nav_label, data jsonb,
       UNIQUE(page_id, anchor))
```
- `slug` зберігати **без** провідного/кінцевого слеша (`''` для home, `services/bouquets` для вкладеної).
- **ІНВАРІАНТ:** зарезервовані slug — єдиними константами. Розподіл (узгоджено з 5.2): matcher виключає з middleware лише `/_next`, `/api` і статичні асети; `sitemap.xml`/`robots.txt` **проходять** middleware (301-перевірка host, без tenant-rewrite — див. 5.2); **іменний** список (`api`, `_next`, `admin`, `sitemap.xml`, `robots.txt`…) звіряється у **валідації конструктора + БД-unique**.

> *Відкинуто:* окрема модель під one-page без сутності «сторінка» — заблокувала б декларований перехід до multi-page. Журнал #4.

### 5.2. ✅ Дворівневий резолвинг
```
Host  → який це tenant      (kvity.com.ua → клієнт "Квіти")  [middleware → canonicalHostname → rewrite /_sites/{host}/...]
path  → яка сторінка tenant (/ → головна, /contacts → контакти)  [optional catch-all [[...slug]]; slug?.join('/') ?? '']
```
- `[[...slug]]` (optional catch-all) матчить і корінь (`slug: undefined`), і будь-яку вкладеність. **ІНВАРІАНТ:** `params` у App Router (Next 15+) асинхронні — спершу `const { host, slug } = await params`, не звертатися до `params.slug` синхронно. Неіснуючий slug → **явний `notFound()`** після запиту в БД (не через `dynamicParams`).
- `export const dynamicParams = true; export async function generateStaticParams() { return [] }` — порожній масив **обов'язковий** (інакше роут стає повністю динамічним, без ISR). Тенанти/сторінки рендеряться on-demand при першому візиті й кешуються.
- ⚠️ **matcher middleware — два різні винятки, не плутати.** Наївний matcher `'/((?!api|_next|[\\w-]+\\.\\w+).*)'` (канонічний vercel/platforms) виключає файли з розширенням **повністю** — і тоді запит `піддомен/sitemap.xml` **ніколи не отримає 301 на канонічний host** (суперечність із 10.2: sitemap/robots житимуть дублями). Правильно: виключати з middleware лише `/_next`, `/api` і статичні асети; `sitemap.xml`/`robots.txt` **пропускати через middleware для 301-перевірки host**, але **не робити** для них tenant-rewrite (їх обслуговують `app/sitemap.ts`/`app/robots.ts` за `headers()`, див. 10).
- ⚠️ Catch-all ловить будь-який сміттєвий глибокий URL → кожен робить запит у БД + `notFound()`. Закласти захист (max-глибина / rate-limit), щоб не довбали БД.

### 5.3. ✅ Навігація — похідна, її НЕ редагують руками
Прямий наслідок рішення «поля, не канвас», і природний наслідок «сторінка = масив блоків» (4.1). Власник не має «редактора меню», який можна зламати — нав генерується з наявних даних, перемикач `tenants.nav_mode`:
- **односторінковий сайт** → пункти нав = **видимі секції** (блоки home з `anchor` + `show_in_nav`, anchor `#services`, `#about`). Сховав секцію (3, hide/show) → пункт зникає з меню автоматично.
- **багатосторінковий** → пункти нав = **опубліковані сторінки** (`pages` з `show_in_nav`, сорт за `nav_order`). Додав/прибрав сторінку → меню оновилось саме.
- Меню завжди консистентне, бо це проєкція даних, а не окрема редагована сутність.

### 5.4. 💡 One-page за замовчуванням (MVP)
**Рекомендація:** дефолт — **односторінковий** сайт (секції + anchor-нав у хедері), не мультисторінковий.
- Для локального бізнесу одна сильна сторінка (hero / послуги / галерея / контакти / заявка) закриває потребу і добре лягає під локальне SEO.
- Нав = скрол до секції, завжди працює; нема биття посилань і осиротілих сторінок; простіша модель для власника; вища надійність генерації.
- **Мультисторінковість** (каталог, окремі сторінки послуг, блог) — закладена в модель даних як **можливість** (5.1.1), але **не в MVP**. Структура `tenant → набір сторінок → набір блоків` уже не унеможливлює перехід пізніше.

### 5.5. ✅ Draft / Published — модель станів `🔶→✅ (2026-07-02)`
Документ у кількох місцях спирався на чернетки (noindex «чернеткових» у 10.4, «нерозпізнане в чернетці» у 3/4.7), не моделюючи їх. Зафіксовано:

- ✅ **Редактор пише в чернетку; відвідувач бачить останню опубліковану версію; кнопка «Опублікувати» = draft→published + purge тегів tenant (9.1).** Одна додаткова кнопка для 50+ — прийнятна ціна за те, що напівзроблені правки ніколи не видно публіці.
- **ІНВАРІАНТ:** публічний рендер читає **лише published**; редактор читає/пише **лише draft**. Дві версії контенту (draft/published на рівні даних сторінки) — конкретна форма (колонки vs версійні рядки) вирішується на етапі дизайну БД, інваріант — ні.
- **Наслідок для кешу:** purge відбувається на **«Опублікувати»**, а не на кожне «Зберегти» — збереження чернетки кеш не чіпає взагалі.
- **Статуси tenant:** `demo/draft` (ще не опубліковано жодного разу → **noindex**, 10.4) → `published` → `suspended` (несплата, state machine білінг+домен, §11).

> *Відкинуто:* «зберіг = одразу live» — відвідувач бачить недоредаговане, кожна правка б'є кеш, і нема куди подіти «нерозпізнане» з 4.7. Журнал #16.

### 5.6. ✅ Воронка заявок → Telegram власнику — ЯДРО цінності `🔶→✅ (2026-07-06)`
⚠️ **Не в MVP v1 — етап 2 (§15).** Власник свідомо відклав; у MVP контакти лише текстом. Нижче — цільова архітектура повного продукту.

Це те, заради чого існує сайт (§1). Без неї продукт — «ще один конструктор». Конкурентна звірка: **пуш заявки власнику — єдина реально відсутня в UA фіча**; сам сайт — комодіті.

**Що бачить КЛІЄНТ на сайті (нуль ботів):**
- Блок `lead_form` — проста форма «Залишити заявку» (ім'я, телефон, повідомлення).
- Контакт-кнопки прямого зв'язку: «Написати в Telegram» (`t.me/…`), «Подзвонити» (`tel:`), пізніше Viber. Це просто лінки.
- ⚠️ Клієнт **НЕ спілкується з ботом** — або заповнює форму, або тисне пряму кнопку. «Напишіть нашому боту» вбиває мотивацію.

**Що отримує ВЛАСНИК (за лаштунками):**
- **Один центральний Telegram-бот платформи** (НЕ бот-на-кожного-клієнта). Власник у дашборді тисне «Підключити Telegram» → лінк → Start → бот зберігає його `chat_id`. Одна кнопка, ~30 секунд.
- Заповнена форма на його сайті → **пуш власнику в його Telegram** (ім'я, телефон, текст). Він тисне на номер і дзвонить/пише.
- **ІНВАРІАНТ:** `chat_id` власника (+ обрані канали контакту) живуть у tenant config (5.1). Один бот обслуговує всіх тенантів; прив'язка — через `chat_id`.
- **ІНВАРІАНТ:** `lead_form` — **єдиний блок зі серверним хендлером сабміту** (форма → server action → пуш через центральний бот на `chat_id`); решта блоків реєстру (4.1) — pure render без бекенду. НЕ реалізувати `lead_form` як декоративну форму.

**Скоуп:**
- ✅ **Telegram-пуш — у MVP.**
- ⛔ **Viber-пуш — НЕ в MVP** (потребує верифікації Public Account + оплата за повідомлення, технічно важчий). Друга хвиля. Контакт-**кнопка** Viber на сайті (лінк) — тривіальна, може бути й раніше.
- Ретеншн: пуш заявки + «сайт реально приводить клієнтів» — головний аргумент, чому власник платить щомісяця (відповідь на «навіщо платити, коли сайт готовий», §11).

> *Відкинуто:* (а) email-нотифікації як основний канал — 50+ в UA не спілкуються з клієнтами через email, а через месенджери; (б) бот-на-кожного-клієнта — зайва складність, один центральний достатній; (в) Viber-пуш у MVP — надто дорогий/складний для старту. Журнал #27.

---

## 6. Домени

Поняття «домен» розпадається на **три різні за складністю задачі**. Не плутати їх.

### Задача 1 — Піддомен (`name.vitryna.com.ua`)
✅ **ВИРІШЕНО: дефолт для всіх.** Тривіально: ми володіємо батьківським доменом, ставимо wildcard `*.vitryna.com.ua` на платформу, роутимо за `Host`, SSL покриває wildcard-сертифікат. Жодного зовнішнього API. Покриває ~80% аудиторії, видається безкоштовно й миттєво.

### Задача 2 — Підключити власний домен клієнта (який у нього вже є деінде)
✅ **Рішення: Cloudflare for SaaS / Custom Hostnames.** Клієнт додає CNAME → один POST на `/zones/{zone_id}/custom_hostnames` → Cloudflare сам випускає й поновлює SSL. Безкоштовний тариф: 100 custom hostnames.
- **Статус у продукті:** запасний шлях «у мене вже є домен». НЕ основний флоу (див. нижче).
- ⚠️ **Економіка на масштабі:** «100 безкоштовно» — не відповідь на 5000 клієнтів. Понад ліміт — ≈$0.10/host/міс, тож навіть ~1000 власних доменів (≈900 платних) ≈ $90/міс — це радше шум, ніж суттєва стаття; але рядок існує й множиться на к-сть клієнтів із власним доменом — врахувати в юніт-економіці (11). Ціну звірити перед фіксацією (тарифи Cloudflare змінюються).

### Задача 3 — Зареєструвати НОВИЙ домен за клієнта
✅ **Зона: `.com.ua` (третій рівень).** Без торгової марки, швидко, дешево.
⛔ **НЕ `.ua` (другий рівень):** вимагає сертифікат торгової марки + напівавтоматична модерація адміністратором 1–14 днів. Для нашої аудиторії це смерть конверсії — автоматизувати неможливо.
- Реалізація: через **reseller-API українського реєстратора** з передоплаченим балансом. Міжнародні реєстратори (Namecheap/GoDaddy) `.com.ua` через API зазвичай не вміють.

**Порядок розробки доменів:** Задача 1 → Задача 2 → (лише за платним попитом) Задача 3.

---

## 7. ✅ Реселінг-флоу реєстрації домену

**Контекст рішення:** клієнту байдуже, *де* зареєстрований домен — йому треба, щоб домен був **підключений до сайту**. Це спрощує архітектуру: якщо домен реєструємо ми, то **ми володіємо nameservers**, і вся CNAME-верифікація з Задачі 2 відпадає для основного флоу.

**Флоу (для клієнта — одна кнопка):**
1. **Чек доступності** — бекенд бʼє reseller-API, повертає вільно/зайнято + ціну.
2. **Альтернативи, якщо зайнято** — ⚠️ критично для аудиторії: введуть `kvity`, буде зайнято. Одразу пропонувати `kvity-lviv`, `kvitylviv`, `kvity2026`. Без цього людина застрягає.
3. **Реєстрація одним викликом** — домен + контакти реєстранта + **наші nameservers** (Cloudflare). Списується з передоплаченого балансу.
4. **DNS вже наш** — додаємо A/CNAME на платформу через Cloudflare API, SSL автоматом. Клієнт нічого не торкається.
5. **Запис у БД** — домен, реєстрант, дата закінчення, ID домену в реселера → у чергу поновлення. (Підключення власного/нового домену → оновити `canonicalHostname` tenant і зробити 301 з піддомену, див. 10.2.)

🔶 **ВІДКРИТЕ — головне рішення перед кодом: хто юридичний реєстрант домену.**
«Без різниці, *де* зареєстрований» (операційно) і «*на кого* зареєстрований» (WHOIS-власник) — різні речі. Для клієнта обидва варіанти виглядають однаково (одна кнопка); різниця проявляється через 1–2 роки.

| Варіант | Плюс | Мінус |
|---|---|---|
| Реєстрант = наш реселер-акаунт | Максимально просто | Клієнт у заручниках; єдина точка відмови (проблеми з акаунтом = всі домени разом) |
| Реєстрант = клієнт (через той самий API) | Домен юридично його; чистий вихід через auth-code; жодної міни | Трохи складніше технічно |

💡 **РЕКОМЕНДАЦІЯ (не підтверджена):** операційно — повний автомат (клієнт нічого не бачить); юридично — **реєстрант = клієнт**. Для `.com.ua` вимоги легкі (контактні дані, без ТМ), тож це реально. Виняток — якщо свідомо обираємо lock-in як стратегію утримання.
⚠️ Переграти постфактум дорого: 5000 доменів на нашому акаунті → «віддати клієнтам» = 5000 трансферів.

🔶 **ВІДКРИТЕ:** хто платить за поновлення і що відбувається при відтоку. Потрібне автосписання в гривні + нагадування, інакше через рік сайти масово відвалюються і бʼють по нас. (Зчеплено з білінгом, 11: щоб поновлювати від імені клієнта, однаково треба токенізована картка + dunning + грейс-період — проєктувати як одну підсистему.)

---

## 8. Реселер-шортліст (вибір не зроблено)

🔶 **ВІДКРИТЕ:** конкретний реселер не обраний. Нижче — кандидати.

| Реєстратор | API | Роль | Нотатки |
|---|---|---|---|
| **ukraine.com.ua** (Хостинг Україна) | Документований HTTP API: `api.ukraine.com.ua/action/hosting/api/<категорія>/<функція>/`, atomic synchronous POST | **Основний кандидат** | ICANN, 432 зони, повна автоматизація; можна скасувати помилкову реєстрацію (рятує при друкарських помилках) |
| **bestname.ua** (besthosting) | HTTP API (JSON), є тестовий режим, dev-підтримка | Запасний / для порівняння | `ns1/ns2/ns3` передаються прямо у виклику |
| NIC.UA | Звірити | Офіційний реєстратор | оплата в UAH |
| Imena.ua | Звірити | Офіційний | позиціонується як провайдер №1 |
| Ukrnames | Звірити | Офіційний | є панель керування |
| uahosting.com.ua | Звірити | Реселер-рівні | статус реселера за запитом від 10+ (I рів.) / 100+ (II рів.) доменів; міжнародні зони через Namecheap |

**Критерії вибору (звірити по доках перед інтеграцією):**
1. **Повнота API:** чек доступності, реєстрація, керування NS, **поновлення (програмне)**, трансфер (auth-code).
2. **Реєстрант = клієнт через API** (щоб виконати рекомендацію з розділу 7).
3. **Кастомні NS у виклику** (щоб одразу садити на наш Cloudflare).
4. **Передоплачений баланс + об'ємні знижки.**

💡 **РЕКОМЕНДАЦІЯ:** ukraine.com.ua основний, bestname запасний; перед інтеграцією звірити три речі — програмне поновлення, передачу реєстранта-клієнта, кастомні NS.

⚠️ **Економіка:** `.com.ua` ≈ $6 USD/рік мінімум для акредитованого реєстратора; суттєві знижки лише при сотнях-тисячах доменів. Тобто на старті домен — майже прохідна стаття без націнки; заробіток — на підписці за конструктор, не на домені. Бонус: за українським законом приватність реєстранта-фізособи у WHOIS безкоштовна.

---

## 9. ✅ Хостинг і модель рендеру

**Вартість визначає НЕ провайдер, а модель рендеру.**

Сайти аудиторії — низькотрафічні, майже не змінюються (телефон раз на місяць). Тому:

✅ **Рендер-модель:** рендеримо сайт у **статику/кеш**, віддаємо з CDN, інвалідуємо кеш конкретного клієнта **тільки на «Опублікувати»** (ISR / on-demand revalidation / purge-on-publish; збереження чернетки кеш не чіпає — див. 5.5). Вартість на візит → майже нуль.
⛔ **НЕ SSR на кожен запит** — інакше платимо compute за кожен перегляд кожного з тисяч сайтів.

### 9.1. ✅ Інвалідація — per-tenant через теги `🔶→✅ (2026-06-30)`
Уточнення purge-механіки за підсумком валідації (тригер — «Опублікувати», 5.5):
- **ІНВАРІАНТ:** інвалідація **per-tenant через теги** (`tenant:{host}`, за потреби `page:{host}:{slug}`), а **не** через шлях-патерн. `revalidatePath('/_sites/[host]/...', 'page')` з динамічним сегментом `[host]` інвалідує кеш **усіх** тенантів — пастка. `revalidatePath` лишити лише для **літерального** destination-шляху конкретної відкритої сторінки.
- Ключ ISR-кешу = **переписаний destination-шлях** (`/_sites/{host}/{slug}`), тож різні сторінки одного тенанта кешуються окремо автоматично.

Базовий (стабільний Next 15) патерн:
```ts
const getPage = (host, slug) => unstable_cache(
  () => db.pages.find(host, slug),
  ['page', host, slug],
  { tags: [`tenant:${host}`, `page:${host}:${slug}`] }
)()
// на «Опублікувати» (не на «Зберегти» чернетки — 5.5):
revalidateTag(`tenant:${host}`)        // весь тенант, не зачіпаючи інших
revalidateTag(`page:${host}:${slug}`)  // одна сторінка
```

⚠️ **Звірити перед інтеграцією — залежить від версії Next.** Наведений патерн орієнтований на **стабільний Next 15.x**, де `unstable_cache` + теги + **одно-аргументний** `revalidateTag` — стабільний шлях, а Cache Components (`'use cache'` / `cacheTag` / `cacheLife`, **дво-аргументний** `revalidateTag(tag, profile)`, `updateTag`) — canary. **У Next 16+ ситуація інвертувалася:** одно-аргументний `revalidateTag(tag)` стає **deprecated**, базовий підпис зміщується на дво-аргументний `revalidateTag(tag, 'max')` + `updateTag` (`revalidateTag(tag, {expire:0})` для негайної інвалідації). **Зафіксувати цільову версію Next перед кодом** і звірити cache-API по docs саме під неї — не кодити з пам'яті/туторіалів.

### 9.2. Стек-провайдер
🔶 **ВІДКРИТЕ** (вердикт-схильність є, не зафіксовано):

| Варіант | За | Проти |
|---|---|---|
| **Cloudflare** (Workers + Cloudflare for SaaS) | Найвирівняніший по вартості; домени вже тут; custom hostnames рідні (100 безкоштовно); дешевий egress; tenant-резолвінг у Worker по `Host` | ⚠️ Next.js на Workers (OpenNext) менш зрілий: ISR/on-demand revalidation/middleware/кеш мають гострі кути |
| **Vercel** (Vercel for Platforms) | Створений під цей сценарій; найкращий DX під Next.js; custom domains через Domains API; tenant-резолвінг у middleware; ISR рідний | Вища ціна на масштабі |

💡 **Вердикт:** при гривневій підписці (економимо копійки) → **Cloudflare-сторона** по вартості. АЛЕ ⚠️ міграція мультитенантного роутингу + ISR між платформами **дорога й ризикована** — «почнемо на CF, мігруємо потім» наївне. Прагматичний гібрид: **застосунок на Vercel (Vercel for Platforms), а домени/SSL/custom hostnames/R2 — на Cloudflare**. Не плати DX-податком у рендер-шарі там, де він найдорожчий. Актуальні ліміти/ціни звірити перед фіксацією.
⚠️ **Нерозв'язана інтеграційна задача гібриду (назвати чесно):** як стикуються wildcard `*.vitryna.com.ua` і CF for SaaS custom hostnames з origin на Vercel (CF-proxy перед Vercel = подвійний CDN зі своїми пастками кешу/SSL). Спроєктувати при фіксації провайдера (§13 крок 8).

**Не плутати з «хостингом застосунку»:**
- ✅ **Дані:** Postgres у **Supabase**, tenant-ізоляція через **RLS**. *(Уточнення: RLS — площина редагування/дашборду. Публічний рендер майже завжди потрапляє в кеш, а не в БД, і читає published-контент сервісним ключем — не плутати RLS з ізоляцією публічного рендеру.)*
- ✅ **Зображення:** **Cloudflare R2** (нульовий egress — критично, бо сайти image-heavy і тисячі сайтів роздають фото). Supabase Storage робочий, але R2 виграє на egress.

**Повний стек end-to-end:**
```
один Next.js застосунок (Vercel; домени/SSL/R2 на Cloudflare)
  → middleware: Host → canonicalHostname → rewrite /_sites/{host}/...
  → tenant config (хедер/футер/нав/тема-токени) + блоки сторінки за slug
  → контент із Supabase (Postgres; RLS — лише площина дашборду, 3.1), читання published обгорнуте в кеш із тегами tenant/page
  → зображення з Cloudflare R2
  → рендер у кеш (ISR on-demand), purge-on-publish через revalidateTag(`tenant:{host}`) (5.5)
  → домени й SSL на Cloudflare
```

---

## 10. ✅ SEO per-tenant

SEO — це **проєкція тих самих даних**, per-tenant, а не окрема фіча, що вмикається збоку. Той самий принцип, що з навігацією: похідна від полів, які клієнт уже заповнив.

**Артефакти, що генеруються на кожен клієнтський сайт (з його ж даних):**
- `<title>` + `meta description` — з назви бізнесу, міста, категорії, послуг. Через `generateMetadata` (Server Component), host з **`params`** (2.4), `metadataBase` **per-tenant** з `canonicalHostname`, URL-поля Metadata API **відносні** (Next зробить абсолют на правильний хост) — **крім JSON-LD: там абсолюти задавати вручну з `canonicalHostname`** (не проходить через `metadataBase`, див. 10.1).
- Open Graph / Twitter card — щоб лінк гарно розгортався у Viber/Telegram/Instagram (канали аудиторії). Динамічні OG-зображення per-tenant через `opengraph-image.tsx` + `ImageResponse` (`next/og`), host теж із **`params`** (інакше зображення стане динамічним і дорогим).
- `sitemap.xml` і `robots.txt` — окремі **на кожен hostname** (`app/sitemap.ts` / `app/robots.ts`, host із `headers()` — єдине легітимне місце; URL будувати з `canonicalHostname`).
- **JSON-LD `LocalBusiness`** (Florist / HairSalon / LegalService…) — назва, адреса, гео, години, телефон, ціновий діапазон. Обов'язкові: `name`, `address`(PostalAddress). Для локального бізнесу — головний шматок.
- `canonical` — на `canonicalHostname`.

### 10.1. ⚠️ Пастка 1 — host-absolute URLs
Для Google кожен hostname — окремий сайт, хоча застосунок один. Canonical, посилання в sitemap, `og:url`, поле `url`/`image` у structured data будувати з **`canonicalHostname`**, **не** з глобального домену і **не** з host запиту. Інакше отруєна індексація клієнта.
- **ІНВАРІАНТ:** глобальний `metadataBase` на кореневий домен **заборонено** — він канонікалізує всі сайти на один хост. Будувати `metadataBase` per-tenant у `generateMetadata`.
- ⚠️ JSON-LD `url`/`image` **не проходять** через `metadataBase` — задавати абсолюти вручну з `canonicalHostname`, стежити, щоб збігалися з canonical.

### 10.2. ⚠️ Пастка 2 — дублі по двох hostname
Якщо сайт доступний і на піддомені, і на власному домені — це дубльований контент.
- ✅ **Рішення `🔶→✅ (2026-06-30)`: 301 з піддомену на `canonicalHostname` у middleware** (директива), а не лише canonical-тег (підказка, яку Google може зігнорувати при ідентичному контенті). `canonicalHostname` — поле в моделі tenant (5.1). Коли клієнт підключає/реєструє власний домен → оновити `canonicalHostname` + увімкнути 301.
- **ІНВАРІАНТ: зміна `canonicalHostname` = кеш-подія `🔶→✅ (2026-07-02)`.** Усі закешовані сторінки містять **старий** canonical/`og:url`/JSON-LD-`url`. При зміні: `revalidateTag('tenant:{старийHost}')` + `'tenant:{новийHost}'`, перегенерувати sitemap, інвалідувати edge-мапу host→tenant (2.5). Без цього §10 отруюється саме в момент апселу власного домену.

> *Відкинуто:* покладатися лише на `rel=canonical` для дублів — слабше за 301; не консолідує вагу надійно. Журнал #9.

### 10.3. Локальне, а не класичне SEO
Львівська квіткарка не виграє по запиту «квіти» і їй це не треба. Працює:
- **Google Business Profile** (map pack) — головний канал, де локальний бізнес реально знаходять.
- **NAP-консистентність** — назва/адреса/телефон ідентичні на сайті й у GBP.
- місто + категорія в `title`/`H1` («Флористика у Львові»).
- Роль сайту: канонічна веб-присутність, яку GBP підтверджує. Для цього сегмента інтеграція з GBP важливіша за класичну on-page.

**Синергія (вже безкоштовно):** статика/кеш (9) → швидке завантаження → добрі Core Web Vitals → ранжуючий сигнал. Архітектура вже працює на SEO.

⚠️ **Чесне обмеження (не продавати магію):** свіжий домен + тонкий AI-текст не вибʼються в топ швидко — у нового домену нуль авторитету, органіка росте місяцями. Реальний швидкий виграш — **GBP-лістинг + технічно чистий індексований сайт**, а не «топ по запиту». Очікування власника керувати в бік локального/GBP.

### 10.4. ⚠️ Ризик масштабу — scaled-content-abuse / репутація кореня `🔶→✅ (2026-06-30)`
Тисячі структурно схожих сайтів із тонким AI-текстом на піддоменах **одного кореня** `vitryna.com.ua` підпадають під Google **scaled content abuse** (заборонено не «AI», а «багато сторінок без цінності для користувача») і **ділять репутацію кореневого домену**. Мітигація (закладати з MVP, не пост-фактум):
- Більше **структурованих фактів** (години/адреса/ціни/фото/відгуки), менше «AI-води»; описи з реальних даних, а не один темплейт із підстановкою назви.
- **`noindex` для порожніх/демо/чернеткових тенантів** — головний запобіжник проти маси тонких сторінок.
- **Підштовхувати активних клієнтів на власні домени + 301 з піддомену** → власний домен **ізолює** репутацію від кореня. Додатковий аргумент за апсел власного домену (6–7).
- Не генерувати сторінки «послуга X у місті Y» сотнями без реального бізнесу за ними.

**Звʼязок із доменом:** власний домен дає чистішу індексацію, ізоляцію репутації й брендову присутність → це підсилює апсел власного домену (6–7). Спільний піддомен — слабше й менш солідно.

🔶 **ВІДКРИТЕ:** інтеграція з Google Business Profile не спроєктована.

---

## 11. ⚠️ Наскрізні ризики (усвідомлені, рішень нема)

- 🔶 **Білінг:** рекурентні платежі в гривні складніші за Stripe (токенізація карт, автосписання, dunning/повторні спроби при відмові). Кандидати: LiqPay / WayForPay / Fondy. Провайдер не обрано. **Зчеплено з поновленням домену (7)** — проєктувати як одну підсистему зі станами `активний → прострочка → грейс → призупинено → видалено`. Елевація: це не «ризик на потім», а кандидат на **прототип №1**, бо гейтить усю модель доходу.
- ⚠️ **Підтримка:** нетехнічна аудиторія = високе навантаження на підтримку («чому не працює», «як поміняти фото»). Операційка вбиває такі продукти частіше за технологію. Часткова мітигація вже в архітектурі: field-модель (3) звужує поверхню «що можна зламати».
- ⚠️ **Юніт-економіка:** домен + хостинг + AI-генерація (особливо картинки) + еквайринг + custom hostnames понад ліміт (6) + SMS для phone-auth (3.1) + Telegram-пуш (дешевий) проти невисокої гривневої підписки = тонка маржа. Модель не порахована. 🔶 **Референс зі старого бізнес-плану** (звірити — рахований під concierge+build-fee, не під чисту підписку): ціна-гіпотеза 199–299 грн/міс; бенчмарки Weblium ~$8≈320 грн (USD-білінг → наша UAH-перевага), Durable $22. Чиста підписка-only на тонкій марджі — головний ризик обраної self-serve-моделі (1.1); ретеншн тримати воронкою заявок (5.6).
- ⚠️ **Anti-abuse:** безкоштовні миттєві сайти на `*.vitryna.com.ua` — магніт для фішингу/скаму, який б'є по репутації кореня **швидше** за SEO-спам (10.4 покриває лише SEO-аспект). 🔶 Модерація / abuse-report не спроєктовані; **мінімум із MVP**: rate-limit на створення тенантів + ручний kill-switch tenant (моментальне зняття сайту з рендеру).

> SEO як ризик «власник очікує бути в Google» закрито окремим розділом 10 (відповідь: керувати очікуванням у бік локального/GBP). Ризик scaled-content-abuse — у 10.4.

---

## 12. Поза скоупом MVP

- Канвас-редактор (свідомо відкладено, див. розділ 3).
- ~~Перемкнути шаблон~~ — **втягнуто в MVP** (3, п.5; `🔶→✅ 2026-06-30`). Більше не в беклозі.
- Мультисторінкові сайти (модель даних дозволяє (5.1.1), але дефолт — one-page, див. 5.4).
- Вільна композиція блоків AI (на MVP — лише пресети-каркаси, 4.2).
- Field-схеми вертикалей (визнано потрібними, не спроєктовано).
- Конкретний дизайн БД під домени + логіка автопоновлення з грейс-періодом.
- GBP-інтеграція (розділ 10).
- **Аналітика/логування** — свідомо НЕ в MVP `🔶→✅ (2026-07-06)`: на стадії частих змін функціоналу логування застаріває швидше, ніж дає користь, а фідбек дає **пряме спостереження за друзями-тестувальниками**. **Тригер увімкнення:** вихід на реальних чужих користувачів, за якими не подивишся. Тоді ж — аналітика для власника («X переглядів, Y кліків на телефон») як retention-важіль.
- **Concierge-модель** (оператор будує сайт за клієнта + build-fee) — свідомо не обрана (1.1); запасний варіант, якщо швидкий шип покаже, що self-serve не конвертує.
- **Viber-пуш заявки власнику** — не в MVP (важкий/дорогий); Telegram перший (5.6). Контакт-кнопка Viber на сайті (лінк) — можна раніше.
- Повноцінна abuse-модерація (у MVP лише rate-limit + kill-switch, §11).
- Phone+SMS auth (у MVP email+пароль, 3.1) — перехід не відкладати надовго.
- Деталі білінг-провайдера обговорювались поза цією сесією і сюди свідомо не зводились.

---

## 13. Рекомендовані наступні кроки

> Переприоритезовано двічі: (v2) спершу де-ризикувати core-loop цінності, потім домени; (v2.1) дизайн-кроки перед end-to-end — бо крок «зібрати вертикаль» залежить від моделі даних, field-схеми й реєстру, а request-flow має бути описаний до першого рядка коду.

1. **Описати request-flow end-to-end** (Host → платформа/tenant (2.5) → canonicalHostname → page → render → cache → publish/invalidate (5.5, 9.1)) — **до першого рядка коду**.
2. **Спроєктувати модель даних:** `tenant config` (бренд/нав/футер/`canonicalHostname`/тема-токени/статуси) × `сторінки` × `блоки` × **draft/published (5.5)** × **членство tenant_members (3.1)**.
3. **Спроєктувати першу field-схему під квіткарню** як еталон + реєстр блоків під неї (4.1, з анотацією fact/creative) + її SEO-проєкцію (розділ 10).
4. **Спроєктувати онбординг-анкету** (джерело фактів 4.4: поля, кроки, генерація піддомену з укр. назви) + **префіл** з Facebook/Instagram/GBP (зменшує введення для 50+ і частково закриває проблему фото).
5. **End-to-end на ОДНІЙ вертикалі (квіткарня):** анкета → класифікація вертикалі → вибір пресету → генерація (реєстр + grounding) → **редагування дотиком-панеллю (3)** → **перемкнути шаблон (4.7)** → фото → **блок заявки + підключення Telegram власника → тестова заявка → пуш (5.6)** → «Опублікувати». Доводить, що петля цінності працює **включно з воронкою заявок** (ядро, не додаток). Зображення — за 4.8 (MVP: реальні фото + обробка на клієнті).
6. **Прототип рекурентного UAH-білінгу** (WayForPay/LiqPay/Fondy) з токенізацією + dunning; спроєктувати спільний state machine білінг+домен (11, 7).
7. Тільки **субдомен** (Задача 1) у MVP; власний домен/реєстрацію (Задачі 2–3, реєстрант, реселер) відкласти до платних юзерів.
8. *(після core-loop)* Зафіксувати провайдера хостингу (гібрид Vercel + Cloudflare, 9.2 — включно з механізмом стикування wildcard/CF for SaaS ↔ Vercel) + цільову версію Next (9.1).
9. *(при платному попиті на домени)* Вирішити реєстранта домену (7) — блокує доменну підсистему, але не критичний шлях MVP.

---

## 14. Журнал рішень (аудит-трейл правок v2/v2.1/v2.2)

Кожен запис — дельта проти попередньої версії. Тіло секцій = поточна істина; цей журнал = «чому/коли/що відкинуто».

| # | Дата | Секція | Тип | Було → Стало | Чому |
|---|---|---|---|---|---|
| 1 | 2026-06-30 | §2.4 (нове) | НОВЕ / ІНВАРІАНТ | — → `canonicalHostname` + host з `params`, не `headers()` | Один інваріант тримає роутинг + SEO без втрати кешу + прив'язку нав. Валідація 3 підсистем. |
| 2 | 2026-06-30 | §9.1 | 🔶→✅ | «purge-on-save» (нечітко) → per-tenant теги, не шлях-патерн | `revalidatePath` з патерном `[host]` б'є по всіх тенантах. |
| 3 | 2026-06-30 | §9.1 | ДОПОВНЮЄ ⚠️ | — → застереження: Cache Components = canary; старт на `unstable_cache` | Не кодити canary-API з пам'яті. |
| 4 | 2026-06-30 | §5.1.1 (нове) | 🔶→✅ / ІНВАРІАНТ | модель даних нечітка → «сторінка» завжди сутність (one-page = slug='') | Гарантує безболісний перехід one-page→multi-page. |
| 5 | 2026-06-30 | §3 (п.5), §12 | 🔶→✅ | «перемкнути шаблон» у post-MVP → у MVP | Клапан тиску під ризик первинної генерації; інакше відмови на онбордингу. |
| 6 | 2026-06-30 | §4.1–4.4 | 🔶→✅ / ІНВАРІАНТ | «система блоків» (загально) → реєстр блоків + 2 рішення AI (пресет+слоти) + структурований вивід + grounding | Закриває галюцинації структури і контенту; робить switchTemplate детермінованим. |
| 7 | 2026-06-30 | §4.3 | ДОПОВНЮЄ ⚠️ | — → structured outputs Anthropic (на момент написання beta — звірити GA/синтаксис); база — tool use input_schema | Точний синтаксис не з туторіалів. |
| 8 | 2026-06-30 | §4.5 | 🔶→✅ / ІНВАРІАНТ | — → тема як design tokens, окремо від контенту | Реалізує обіцянку 2.1 «перегенерувати дизайн не торкаючись контенту». |
| 9 | 2026-06-30 | §10.2 | 🔶→✅ | «301, мінімум canonical» → 301 у middleware основний | 301 директива > canonical підказка. |
| 10 | 2026-06-30 | §10.4 (нове), §11 | НОВЕ ⚠️ | — → ризик scaled-content-abuse / репутація кореня + мітигації | Реальна політика Google; `noindex` порожніх + власні домени. |
| 11 | 2026-06-30 | §9.2 | ДОПОВНЮЄ | «CF, мігруємо потім» → гібрид Vercel(app)+Cloudflare(домени/R2) | Міграція роутингу+ISR між платформами дорога; OpenNext менш зрілий. |
| 12 | 2026-06-30 | §6 (Задача 2), §11 | ДОПОВНЮЄ ⚠️ | «100 hostnames безкоштовно» → рядок собівартості ≈$0.10/host понад ліміт (шум, але існує) | На 5000 клієнтів вийде за безкоштовний ліміт; порядок ціни уточнено. |
| 13 | 2026-06-30 | §13 | ПЕРЕКРИВАЄ | порядок кроків (домени перші) → core-loop цінності перший | Домени — апсел, не основна петля; де-ризикувати головне. |
| 14 | 2026-07-02 | §2.5 (нове) | 🔶→✅ / ІНВАРІАНТ | — → платформа на `app.`, порядок middleware, 404 для невідомого host, edge-мапа host→tenant (не Postgres на запит) | Fresh-eyes ревю: перші рядки middleware не можна було написати без вгадування. |
| 15 | 2026-07-02 | §3.1 (нове), §11, §12 | 🔶→✅ | — → auth фазово: email+пароль (MVP) → телефон+SMS; `tenant_members` замість owner_id; SMS у юніт-економіці; перехід у беклозі | Рішення користувача; членство дешеве зараз, дороге потім. |
| 16 | 2026-07-02 | §5.5 (нове), §9 | 🔶→✅ / ІНВАРІАНТ | «purge-on-save» → draft/published + purge на «Опублікувати»; статуси tenant | Документ спирався на чернетки, не моделюючи їх; «зберіг=live» показує недоредаговане. |
| 17 | 2026-07-02 | §4.7 (нове) | ДОПОВНЮЄ | «перемкнути шаблон» (✅ без механізму) → алгоритм: мапінг за blockType, кишеня чернетки, тема від пресету, все в draft | ✅ без алгоритму = агент вгадує. |
| 18 | 2026-07-02 | §10.2 | ДОПОВНЮЄ / ІНВАРІАНТ | — → зміна canonicalHostname = кеш-подія (purge старий+новий host, sitemap, edge-мапа) | Інакше SEO отруюється в момент апселу домену. |
| 19 | 2026-07-02 | §5.2 | ПЕРЕКРИВАЄ | наївний matcher (виключає всі файли) → sitemap/robots проходять 301-перевірку, але без tenant-rewrite | Суперечність §5.2↔§10.2: піддоменний sitemap.xml ніколи не отримував 301. |
| 20 | 2026-07-02 | §4.4 | ДОПОВНЮЄ | — → анотація полів fact/creative + нормалізація перед порівнянням; онбординг-анкета позначена 🔶 | Без анотації «пост-валідація фактів» нереалізовна; переформатування ≠ вигадка. |
| 21 | 2026-07-02 | §11, §12 | ДОПОВНЮЄ ⚠️ | — → anti-abuse (rate-limit + kill-switch у MVP, решта 🔶); аналітика власника — свідомо post-MVP | Фішинг на піддоменах б'є по кореню швидше за SEO-спам; аналітика = retention. |
| 22 | 2026-07-02 | §13, §9.2 | ПЕРЕКРИВАЄ | end-to-end першим → дизайн-кроки (request-flow, модель даних, field-схема, анкета) перед end-to-end; названа інтеграційна задача гібриду | Крок 1 залежав від кроків 5–6; request-flow має передувати коду. |
| 23 | 2026-07-06 | §1 | 🔶→✅ | «аудиторія = 50+» → цільовий платник = **нетехнічний будь-якого віку**, 50+ = лінза дизайну, не гейт | Рішення власника; знімає суперечність «50+ не конвертують у self-serve». Звірка зі старим бізнес-планом. |
| 24 | 2026-07-06 | §1 | ДОПОВНЮЄ | теза «складність = інфра» → **ядро цінності = воронка заявок**; AI-генерація = table-stakes; UAH-білінг/кнопки = гігієна | Стара конкурентна звірка: пуш заявки — єдина реальна прогалина UA-ринку. |
| 25 | 2026-07-06 | §1.1 (нове) | 🔶→✅ | модель монетизації неявна → **self-serve явно обрано** (не concierge), ship-fast, ризик прийнято свідомо | Рішення власника; concierge — запасний варіант. Бриз де-факто був self-serve, тепер це назване. |
| 26 | 2026-07-06 | §3 | 🔶→✅ | «редагування полями (CMS-стиль)» → **натиснув на елемент → панель знизу з полем**; заборона drag/resize | Рішення власника; тепліший UX, лишається безпечним для нетехнічних. |
| 27 | 2026-07-06 | §5.6 (нове), §5.1, §4.2 | 🔶→✅ / ІНВАРІАНТ | воронки заявок не було в архітектурі → **форма/кнопки → Telegram-пуш власнику як ЯДРО MVP**; `telegram_chat_id` у tenant config; `lead_form` у кожному пресеті | Найбільша прогалина зі звірки; це те, заради чого сайт існує. |
| 28 | 2026-07-06 | §12, §13, §11 | ДОПОВНЮЄ | аналітика/логування НЕ в MVP (тригер — чужі юзери); Viber-пуш і concierge — out; крок 5 включає воронку заявок; референс-економіка зі старого плану | Рішення власника (ship-fast, фідбек від друзів); підбір корисного зі старих документів. |
| 29 | 2026-07-06 | §4.8 (нове), §4.6 | 🔶→✅ | зображення відкрите → MVP: реальні фото + HEIC/EXIF/модерація/focal-point + заглушки; AI лише трансформує реальне (v1.1); фабрикація товару — ніколи | Рішення власника «не блокер»; трансформувати реальне ≠ вигадувати неіснуючий товар. |
| 30 | 2026-07-06 | §4.9 (нове), §4.4 | 🔶→✅ | онбординг-анкета → **агент-чат** (slot-filling за схемою + підтвердження-форма + персистентність) | Рішення власника; чат тепліший + demo-wow, схема лишається джерелом істини. |
| 31 | 2026-07-06 | §15 (нове), §4.2, §5.6 | 🔶→✅ | зафіксовано **MVP-скоуп v1**: агент-чат IN; lead_form/Telegram/білінг/реєстрація-доменів/анкета OUT (етап 2); стек Vercel+піддомени | Ship-fast MVP для тесту генерації на друзях. |
| 32 | 2026-07-06 | §15 | ⚠️ РИЗИК | названо свідомо: MVP тестує генерацію (комодіті), НЕ цінність; ядро (заявка→Telegram) відкладене в етап 2 | Fable-валідація; власник прийняв ризик, етап 2 тримати «тижні, не місяці». |

**Не змінено (усі валідації пройшло):** хребет 2.1–2.3, мультитенантність, Модель A рендеру, поля-не-канвас (вісь; спосіб входу оновлено #26), дворівнева модель, нав-похідна, домени (3 задачі), реселінг-флоу. **Закрито у v2.2:** бізнес-модель (self-serve, 1.1), цільовий клієнт (1), канал заявок (Telegram, 5.6 — у повному продукті), зображення (4.8), онбординг-механізм (агент-чат, 4.9), MVP-скоуп (§15). Досі відкрите: реєстрант домену (7), реселер (8), білінг-провайдер (11), GBP (10), **field-схема першої вертикалі (§13 крок 3 — критичний шлях MVP)**, abuse-модерація повна (11), стек-провайдер фіксація на масштабі + механізм гібриду (9.2, post-MVP).

---

## 15. ✅ MVP-скоуп v1 (спринт) `2026-07-06`

Рішення власника в режимі **ship-fast**. Мета MVP — **довести генераційну петлю через агент-чат на живих сайтах для друзів-тестувальників.**

**IN (у MVP):**
- **Стек:** чистий **Vercel**, лише піддомени `*.vitryna.com.ua` (wildcard → Vercel, налаштувати раз). Без Cloudflare-гібриду (він для власних доменів — post-MVP). Зображення — Supabase Storage / Vercel Blob (R2 потім).
- **Агент-чат** (4.9) → структурована схема → підтвердження-форма → **генерація** (реєстр блоків + grounding, §4.1–4.4) → **БД** (Supabase) → **мультитенант-рендер** на піддомені (§2.2, §5.2) → **редагування дотиком-панеллю** (§3) → **«Опублікувати»** (§5.5).
- **Field-схема + реєстр блоків під ОДНУ вертикаль (квіткарня)** — критичний шлях, будувати першою.
- **Зображення** — завантаження реальних фото + обробка на клієнті HEIC/EXIF (§4.8 MVP-мін). Модерацію/auto-enhance/focal-point для тесту на друзях можна відкласти.
- **Контакти — текстовий блок** (телефон/адреса як текст).
- **Auth** — email+пароль (§3.1), `tenant_members`.

**OUT / етап 2+ (свідомо):**
- ⚠️ **lead_form + контакт-кнопки + Telegram-пуш заявок** (§5.6) — **етап 2**. У MVP контакти лише текстом. Це відкладає **ядро цінності** (§1): MVP тестує генерацію/онбординг (комодіті, «~20% продукту»), **НЕ** цінність і **НЕ** готовність платити. Свідомо — воронку на друзях без трафіку не перевіриш.
- Білінг, реєстрація власних доменів, красива форма-анкета (замінена агентом), аналітика/логування, Viber, concierge, мультисторінковість.

⚠️ **Найбільший усвідомлений ризик:** якщо етап 2 (заявка→Telegram) затягнеться, продукт застигне як «ще один AI-генератор» — те, від чого бриф свідомо тікав. **Тримати етап 2 як «тижні, не місяці».**

> Порядок реалізації — у `mvp-build-plan.md`.
