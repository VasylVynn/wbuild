# План: розумніший чат + вибір шаблону в розмові + повторні секції + SEO + Instagram/Apify

> **Дата:** 2026-07-17. **Статус:** на затвердження власником.
> **Процес:** запит власника → 2 research-агенти (gen-map: конвеєр генерації/шаблони/дублікати/SEO; ig-map: онбординг/медіа/Apify) → синтез. Всі file:line нижче — перевірені агентами по коду.
> **Мандат власника:** чат має ВІДЧУВАТИСЬ дуже розумним; без жорстких рамок збору — якщо AI бачить, що ще трохи інформації підніме якість сайту, хай питає (питання прості, зрозумілі); чат знає шаблони з початку розмови, обирає найкращий і може передумати посеред розмови; якщо контенту багато — генерація може використати ту саму секцію двічі, сайт має бути гарним і SEO-оптимізованим; Apify-інтеграція для Instagram.

---

## Хвиля A — Вільний «розумний» збір фактів (~0.5 дня)

Сьогодні жорсткість сидить у трьох рядках промпту (`lib/ai/onboard.ts`):
список полів як «порядок денний» (`:107-108`), батчинг (`:117`), і головне — **стеля «максимум 2 поглиблювальні питання»** (`:121`).

- [x] **A1.** Прибрати стелю `:121`. Нове правило: «Кількість питань визначаєш ТИ: якщо ще одне-два питання помітно піднімуть якість сайту — питай. Орієнтир — цінність для сайту, не чек-лист.»
- [x] **A2.** Правило простоти: «Кожне питання — одна проста думка, побутовою мовою, без термінів. Ніколи два складні питання в одному повідомленні.»
- [x] **A3.** Правило поваги до темпу: короткі/нетерплячі відповіді або «просто згенеруй» → негайно вести до ready (існуючий ready-gate `:123` лишається). Переформулювати `:107-108`: список полів = мінімум, не стеля.
- [x] **A4.** НЕ чіпати: залізне правило «?» (`:124`), `fallbackQuestion` (`:216-227`), стрімінг-guard (`route.ts:125-127`). _Нотатка: гілка wave-A створена від wave-H1-H5 (не від master) — H4 уже правив той самий промпт-файл; стек мержиться після H1–H5._
- [x] **A6. Чат-підтвердження замість форми (вимога власника, ~0.5–1 день).** _Нотатки виконання: (1) статус став трьохстановим `collecting → ready → confirmed` у save_facts; «ready» = резюме+питання підтвердження, «confirmed» = явна згода → кнопка «Додати фото й створити сайт» → media-крок → gate → одразу finalize; (2) side-fix цін: корінь уже здіагностовано в H4 — цін не було в ДАНИХ (промпт їх не збирав), форма показувала колонку коректно; (3) бонус-фікс: стара форма мовчки ГУБИЛА viber/telegram/testimonials/socials при finalize (збирала лише свої 7 полів) — прямий finalize передає всі факти; (4) фінальна фраза fallbackQuestion оновлена (посилалась на кнопку, якої більше нема у тому стані) — сам механізм не чіпався; (5) refusal-ходи (rate limit) більше не скидають ready/confirmed на клієнті; (6) `confirmed` персиститься у facts_state і відновлюється при resume._ Сьогодні перед генерацією користувача викидає у форму-анкету (`OnboardChat` phase "confirm") — це ламає розмовний флоу, і власник помітив, що ціни послуг там не видно. Заміна:
  - коли `ready` — агент шле СТРУКТУРОВАНЕ РЕЗЮМЕ в чат: назва, місто, телефон, адреса, години, послуги **З ЦІНАМИ**, про нас, к-сть фото/лого, обраний дизайн (B2) — і питає «Все вірно, чи щось замінити?» (quickReplies: «Все вірно, генеруємо» / «Хочу виправити»);
  - правки — далі розмовою (last-wins у facts вже працює), після кожної правки — оновлене резюме коротко;
  - обовʼязково додати фразу: «Після генерації ви зможете змінити будь-який текст чи секцію — самі в редакторі або попросивши асистента»;
  - кнопка «Створити сайт» зʼявляється лише після явного підтвердження; media-крок (фото/лого) лишається перед ним;
  - форму-анкету видалити (валідацію обовʼязкових полів несуть існуючі validators + ready-gate: без телефону/назви/міста агент не дасть ready);
  - side-fix: перевірити, чому в поточній формі не відображались ціни послуг (діагностика перед видаленням — раптом баг сидить у facts, а не у формі).
- [x] **A5. Питання про лого і фото (вимога власника).** _Нотатка: прапорці додані в `businessFactsSchema` (їдуть звичайним factsPatch), перед `generateAndPublish` відсікаються у `finalizeAction` — у генерацію і `tenants.facts` не потрапляють; media-крок показує м'які підказки при hasLogo/hasPhotos === false._ Агент у розмові ПИТАЄ: «Чи маєте логотип?» і «Чи є фото ваших робіт / закладу?» (прості так/ні питання, quickReplies «Так / Ні / Немає, але хочу»). Відповіді → у факти (нові прапорці `hasLogo?`, `hasPhotos?` у facts_state або зберігати як частину розмови):
  - «так» → агент: «Чудово — на наступному кроці попрошу їх завантажити» (media-крок уже існує);
  - «нема лого» → media-крок не наполягає на лого; агент згадує, що шапка працює і з текстовою назвою;
  - «нема фото» → агент чесно попереджає, що згенеруємо атмосферне hero-зображення (runtime-генерація вже вміє), і порадить, які фото зробити пізніше.

> **Нотатка хвилі A (2026-07-17, verification + adversarial review):** E2E-прогін Playwright (логін → вільна розмова → питання про лого/фото → резюме з цінами → підтвердження → CTA → media-крок з підказками → генерація) — 7/7 PASS, скріншоти в сесійному scratchpad; тестовий сайт `kvitkova-maisternia-pivoniia.lvh.me` лишився в базі (можна видалити разом із `wave-smoke`). Codex-рев'ю: 1 must-fix підтверджено і виправлено трьома шарами — (а) жорсткий код-гейт у `parseOnboardMessage`: без назви/міста/телефону статус примусово даунгрейдиться до collecting (fallbackQuestion сам питає перший відсутній факт), (б) `handleFinalize` при спрацюванні захисту скидає confirmed і каже в чаті, чого бракує (був мовчазний вічний bounce), (в) серверний backstop у `finalizeAction`: `businessFactsSchema.safeParse` + непорожня трійка перед генерацією. Nit прийнято без фіксу: `/api/dev/*` роути не стрипають hasLogo/hasPhotos — вони NODE_ENV-гейтнуті.

## Хвиля B — Чат знає шаблони, обирає і може передумати (~1 день)

Ключова знахідка gen-map: `generateSite` **вже приймає** `templateId` 5-м параметром і форсує його end-to-end (`generate.ts:167-180, :242`) — «Перегенерувати» цим уже користується. Бракує лише прокидки згори.

- [x] **B1.** Каталог шаблонів у промпт онбордингу: `label` + `description` (настрій) + `verticalIds` з `lib/templates/registry.ts:50-71` — стислим блоком «ДОСТУПНІ ДИЗАЙНИ». Після класифікації вертикалі — підказати `templatesFor(verticalId)` (`registry.ts:179`).
- [x] **B2.** `save_facts` (`onboard.ts:26-31`): додати `templateId?` (enum з ідентифікаторів каталогу). Правило в промпті: «Обирай дизайн, щойно відчув характер бізнесу; якщо нові факти міняють картину — можеш ЗМІНИТИ вибір будь-якого ходу (last-wins). Можеш коротко сказати власнику, який стиль обрав і чому.»
- [x] **B3.** `FactsState` (`persist-actions.ts:10-16`): + `templateId?`; зберігати в `saveTurn`.
- [x] **B4.** Прокидка: `finalizeAction` (+`templateId`) → `generateAndPublish` (`publish.ts:22-31`, + параметр) → `generateSite` (слот існує). Нема вибору від чату → поведінка як зараз (модель обирає при генерації).
- [x] **B5.** UI: чип обраного дизайну в `SitePreviewPanel` («Дизайн: Студія — темний преміум») — живе відчуття «він уже думає про вигляд».

> **Нотатка хвилі B (2026-07-17, verification + adversarial review):** гілка wave-B створена від wave-A (B правив той самий onboard.ts; стек мержиться H1-H5 → A → B). Реалізація: `templateId` їде ОКРЕМИМ полем save_facts (не в factsPatch — це не бізнес-факт), у tool-схемі enum, на парсі lenient string + валідація проти registry (галюцинований id не топить facts-патч); лейбл для UI резолвиться на сервері (`templateDisplayName`), клієнт registry не бандлить (перевірено: /app/new лишився 134 kB first-load). E2E-прогін Playwright: реальна розмова (кав'ярня, Львів) → агент обрав `restaurant` з ПЕРШОГО ходу і природно сказав про це в чаті → чип «Дизайн: Restaurant — тепла гостинність» у SitePreviewPanel → templateId у facts_state → резюме з рядком **Дизайн:** → підтвердження → генерація → тенант `kava-bilia-ratushi.lvh.me` отримав brand.templateId="restaurant" (packId null), сайт рендериться шаблоном restaurant. Тестовий тенант лишився в базі (можна видалити разом із попередніми smoke-тенантами). Codex-рев'ю: 2 must-fix виправлено (фолд по ВСІХ save_facts-блоках замість першого; refusal нестрімового fallback більше не скидає ready/confirmed/template — echo через опційний параметр). Прийнято свідомо: неатомарний read-modify-write у saveTurn (прееxisting патерн media-merge; UI серіалізує ходи, транзитний stale лікується наступним ходом). Дрібне відхилення: формулювання forced-template у промпті генерації узагальнено (було «(регенерація)» — тепер покриває і вибір чату).

## Хвиля C — Повторне використання секцій (~1 день)

gen-map знайшов рівно три точки, що блокують дублікати:

- [x] **C1.** Зняти кап «1 блок на секцію» в `assemble` (`generate.ts:359-364`) → дозволити до **2** на секцію (запобіжник від розносу). Для pack-шляху підняти `maxPerPage` де доречно. _Нотатка: кап 2 діє лише для секцій З layout-варіантами (ризик №1 плану); додатковий детермінований пост-прохід переназначає повтору перший невикористаний layout, якщо модель повторила той самий; pack-шлях — лише services 1→2._
- [x] **C2.** Унікальні DOM id: `PageRenderer.tsx:46` — для повторної секції додавати лічильник (`#services-2`), як уже робить pack-шлях (`generate.ts:495-503`). Інакше — дубльовані `<section id>` і биті якорі.
- [x] **C3.** Навігація: дедуп по section id вже є (`app/s/.../page.tsx:99-107`) — перевірити, що якір веде на перший інстанс; лишити одну мітку в nav. _Нотатка: template-шлях уже дедупив (buildTemplateBrand); доданий дедуп по label у класичний getNav._
- [x] **C4.** Промпт генерації: «Якщо контенту багато — можеш використати секцію двічі (напр., послуги: основні + додаткові), але СУСІДНІ повтори мусять мати РІЗНІ variants/skins і різні заголовки». Це і краса, і анти-дубль для SEO.
- [x] **C5.** Едж-тест редактора: reorder/hide/agent-tools з дублікатами (індекси позиційні — має працювати; перевірити skin-пікер і frame-превʼю). _Нотатка: агент-тули (add/hide/show/move) прогнані наживо з дубльованим services — позиційні індекси коректні; frame-превʼю рендерить; skin-пікер на template-сайтах не показується за дизайном (шаблон володіє виглядом), set_skin — той самий позиційний executor. Побічне спостереження (поза скоупом): add_block редакторського агента не ставить section/anchor — доданий блок рендериться шаблонною секцією, але без DOM id і без пункту nav._

> **Нотатка хвилі C (2026-07-17, verification + adversarial review):** гілка wave-C від wave-B (C1/C4 правлять generate.ts; стек H1-H5 → A → B → C). Живий E2E: реальна генерація з 12 послугами на форсованому salon дала **services ×2** в одній секції — різні variants (default / rows), різні заголовки («Меблі на замовлення» / «Додаткові послуги»); публічний DOM: ids `services` / `services-2` унікальні, nav — одна «Послуги» → перший інстанс; тенант `wave-c-smoke.lvh.me` лишився в базі для огляду. Агент-тули редактора прогнані з дубльованим services на `kava-bilia-ratushi` (add/hide/show/move — позиційно коректно, draft-only). Тактичні відхилення: (1) кап 2 лише для секцій З layout-варіантами + детермінований пост-прохід «повтор ≠ той самий layout» (страховка понад промпт-правило C4); (2) pack-шлях — лише services 1→2; (3) `/api/dev/generate` навчився `templateId` (dev-only, для смоків). Codex-рев'ю: 2 must-fix виправлено (validAnchors тепер рахує section-ids так, як їх РЕНДЕРИТЬ PageRenderer — лінк на #services-2 більше не переписується у форму; дедуп nav по базовому якорю замість label). Відомий борг (should-fix, прийнято): нумерація якорів класики (по типу, computePlacement) і DOM-ids шаблонів (по секції, PageRenderer) — два окремі алгоритми; на template-сайтах stored anchor не використовується як DOM id, тож контракт цілісний, але централізація — кандидат на окремий рефакторинг. Fullpage-скріншоти салону порожні нижче фолда — відома H5-поведінка reveal (при скролі контент є), не регресія C.

## Хвиля D — SEO-оптимізована генерація (~1–1.5 дня)

Що вже добре: canonical/OG/JSON-LD/robots/sitemap per-tenant, чиста ієрархія (один H1). Конкретні діри (gen-map §5):

- [x] **D1.** SEO-поля в `build_site` (`generationSchema`, `generate.ts:41-59`): + `seo { title, description }` з директивою «title: {послуга} у {місто} — {назва}, ≤60 симв.; description: продаюча, ≤150 симв., з містом і нішею». Зберігати в `brand`, використовувати в `page.tsx:33` і `seo.ts:82-88` (зараз description часто ПОРОЖНІЙ). _Нотатка (тактичне відхилення): зберігається НЕ в brand, а в page content (`draft_content.seo` → `published_content.seo`) — brand неверсіонований і зміни в ньому одразу live, що ламало б інваріант №6 для майбутнього set_seo-тула агента (D5); page-шлях дає draft→publish версіонування безкоштовно. Довжини не капляться в zod (overshoot моделі не має валити генерацію) — детермінований clamp 70/170 у коді. Усі draft-райтери (saveDraftBlocks, regenerateSite, switchDesignPack) навчені round-trip'ити seo; publishSite промоутить його._
- [x] **D2.** JSON-LD (`seo.ts:94-126`): + `openingHours` (з hours), `priceRange` (з services), `sameAs` (з socials/instagram). Дані вже у фактах — не рендеряться. _Нотатка: freeform-години конвертує детермінований парсер `parseOpeningHours` (Пн/повні назви/діапазони/«щодня»/«будні»/«цілодобово»/«з 9 до 18»); емітиться ЛИШЕ впевнено розпарсене, решта мовчки пропускається («за записом» → нічого) — графік ніколи не вигадується. priceRange = min–max по числах із цін послуг («від N грн» при одному числі з «від»). sameAs — http(s)-лінки з socials + захисне читання facts.instagram (повний URL або хендл; поле формально зʼявиться у хвилі E). Санітарний прогін парсера: 15/15 кейсів очікувано (bun, scratchpad)._
- [x] **D3.** Alt-тексти — одразу правильно (без «мінімального» етапу): поле `alt` в image-полях схеми блоків, модель пише описовий alt при генерації, G-аналіз пропонує alt для завантажених фото. Зміна словника блоків — координувати з template-агентом. _Нотатка (тактичне відхилення): модель НЕ пише alt для фото — вона їх не бачить (§4.8), опис невидимого фото = вигадка. Alt детермінований з фактів: hero-фото → «Назва, Місто», generatedHero → «Атмосферне зображення — …», галерея → «Назва, Місто — фото N»; послуги/switchback у рендері беруть alt із name/heading. Нове поле `imageAlt` у heroSchema (форма редактора підхопила автоматично через fields.ts). Справжні описові alt від vision — гачок для G2. Словник: gallery.alt уже існував; template-зона перевірена (git status чистий) — правки рендерерів механічні, alt-only._
- [x] **D4.** SEO-директива в промпт генерації (`generate.ts:140-158`): заголовки секцій з ключовими словами ніші+міста, без спаму. _Нотатка: місто — природно в 1–2 місцях сторінки (hero-підзаголовок/про нас), не в кожному заголовку; ключові слова ніші — в частині заголовків секцій, образність лишається в підзаголовках._
- [x] **D5.** Ролі SEO-порадника агента редактора: підказувати D1-поля при редагуванні (він уже вміє радити — дати йому tool на seo-поля після D1). _Нотатка: tool `set_seo` (title/description, merge-семантика, порожній рядок очищає поле) → server action `saveDraftSeo` → draft_content.seo, БЕЗ purge кешу (draft-only, інваріант №6); live стає лише після «Опублікувати». Поточні seo-значення показуються агенту в system-контексті._

> **Нотатка хвилі D (2026-07-17, verification + adversarial review):** гілка wave-D від wave-C (D1/D3/D4 правлять generate.ts; стек H1-H5 → A → B → C → D). Живий E2E: реальна генерація (autoservice, форсований studio, `wave-d-smoke.lvh.me`) → title «Ремонт авто у Львові — СТО Майстер», description з містом/нішею, JSON-LD з `openingHours ["Mo-Fr 09:00-19:00","Sa 10:00-15:00"]`, `priceRange "350–900 грн"`, `sameAs` (Instagram), атмосферний hero з чесним alt; місто природно сидить в hero-eyebrow (D4). Живий smoke D5: агент редактора на `kava-bilia-ratushi` викликав set_seo → title/description у `draft_content.seo`, `published_content.seo = null` — інваріант №6 підтверджено базою. Парсери: 21/21 (години) + 11/11 (ціни) санітарних кейсів. Codex-рев'ю: 2 must-fix виправлено (кома-списки днів «Пн, Ср, Пт 9-18» більше не гублять дні — pend-механіка з жорстким правилом «тільки чисті день-сегменти», щоб «Нд — вихідний» не успадкував час; wrap-around «Пт–Пн» скіпається замість неповного розкладу; priceRange якориться до валюти — «15 троянд — 1200 грн» більше не дає підлогу 15 грн) + 1 should-fix (final SSE editor-chat тепер несе seo). Прийнято без фіксу: dev-only seed route переписує content без seo (це його робота — чистий reset). Побічні спостереження (поза скоупом): (1) studio nav-мітка «Смуга-тверджень» для stats-секції — незграбна назва, кандидат у H7-аудит; (2) smoke-тенанти `wave-d-smoke`, `wave-c-smoke`, `kava-bilia-ratushi`, `kvitkova-maisternia-pivoniia`, `wave-smoke` досі в базі — можна видалити пачкою. Технічна засторога: `npm run build` під час запущеного `next dev` ламає його `.next`-кеш (усі роути 500) — після build перезапускати dev._

## Хвиля E — Instagram-first онбординг: лінк → скрейп → підтвердження → прогалини (~2.5–3 дні)

> **Редизайн 2026-07-20 за рішенням власника** (спека:
> `docs/superpowers/specs/2026-07-20-instagram-first-onboarding-design.md`).
> Було: агент питає хендл посеред розмови і пропонує імпорт. Стало: Instagram —
> ВІДКРИВАЮЧИЙ хід онбордингу. Привітання пропонує кинути посилання; блокуючий
> спінер зі стадіями (~1–2 хв, вибір власника — «чесне очікування» замість
> fire-and-continue); зведене повідомлення «ось що я дізнався» + «Все вірно?»;
> далі агент питає лише прогалини (телефон, години, адреса). Рілси: обкладинка =
> фото, підпис = текст. Fail-open усюди; без `APIFY_API_TOKEN` фіча не згадується.

- [x] **E1.** `businessFactsSchema`: + `instagram?: string` (поруч із viber/telegram). `socials[]` вже існує — сумісно.
- [x] **E2. IG-хелпери лінків** — `lib/blocks/contact-links.ts`: `normalizeIgHandle(raw)` (хендл / «@хендл» / повний URL → хендл, валідація `[a-z0-9._]{1,30}`), `instagramHref(raw)`. Чисті функції, переюзаються клієнтом (E7), рендером (E9) і apify-клієнтом (E4).
- [x] **E3. Серверний імпорт фото** — `lib/media/import.ts`: `importExternalImage(url, {conversationId|host}) → storageUrl | null`: fetch (таймаут, 8MB, image-mime) → `qualityPass`/sharp WebP (existing, `process.ts`) → bucket upload + publicUrl (за зразком `upload/route.ts:100-129`). Вихід проходить §4.8 `isStorageUrl` автоматично. Fail-open null.
- [x] **E4. Apify-клієнт** — `lib/ig/apify.ts`: актор `apify/instagram-profile-scraper` в АСИНХРОННОМУ режимі (start run → полінг статусу → датасет; синхронний run-sync не влазить у таймаути), `APIFY_API_TOKEN` через lazy env-read (патерн `getAnthropic()`), токен ніколи не логуються. Вихід: біо, повне імʼя, аватар, 10–15 останніх постів (у рілсів — displayUrl-обкладинка + caption). **Fail-open** — будь-який збій → null.
- [x] **E5. LLM-екстракція фактів** — `lib/ig/extract.ts`: біо + підписи постів → factsPatch-кандидати (businessName, about, послуги з цінами, місто) через tool-схему з жорстким анти-вигадуванням: лише те, що написано ДОСЛІВНО; телефони/реквізити НЕ витягуємо (їх агент питає явно). Один виклик на імпорт.
- [x] **E6. SSE-роут `/api/ig-import`** (патерн `/api/onboard`, подовжений `maxDuration`): rate limit `ig_import` (новий LimitName, ~10/добу) → Apify (E4) → паралельний імпорт фото (E3) + vision-аналіз кожного (existing `analyzePhoto`, хвиля G) → екстракція (E5) → стріми стадій («Відкриваю профіль…» → «Читаю пости…» → «Розкладаю фото…») → фінальний payload: BatchItem[] (формат чат-аплоаду) + factsPatch-кандидати + хендл.
- [x] **E7. Клієнт (OnboardChat)**: привітання з IG-запрошенням + чип «У мене є Instagram»; детекція instagram.com-URL / голого @хендла у надісланому повідомленні → імпорт ЗАМІСТЬ звичайного ходу агента (повідомлення лишається в історії); блокуюча картка-спінер зі стадіями з SSE; фінал → існуючий `routeBatch` (лого лише якщо ще нема, галерея ≤8, меню, відгуки→OCR-картки) + зведене повідомлення + «Все вірно?» (чипи); факти одразу в factsPatch (чипи прогресу + SitePreviewPanel оновлюються); mid-chat кнопка «Підтягнути з Instagram», коли `facts.instagram` зʼявився без імпорту; fail → мʼяке повідомлення і класична розмова.
- [x] **E8. Промпт онбордингу**: правило привітання/першого питання з IG (умовне — лише коли токен налаштований); після імпорту НЕ перепитувати відоме (факти + media-інвентар G4 вже покривають), питати ПРОГАЛИНИ по одному (телефон → години → адреса); хендл у `factsPatch.instagram`, якщо користувач згадав IG текстом.
- [x] **E9. Рендер лінка на сайті**: `contactsSchema` (+instagram), `factPaths.contacts`, кнопка в `Contacts.tsx` (через `instagramHref` з E2), grounding у `groundAndPlace` + `allowedFactHrefs` (лінк на профіль з фактів — легальний href, зчеплення з H3); `TemplateBrand.contact` + футери шаблонів (IG-іконки з `href:"#"`) підключити до фактів. Зараз socials не рендеряться ніде — закриває діру.
- [x] **E10.** `.env.example`: + `APIFY_API_TOKEN`; Vercel env. Без токена фіча тихо вимкнена (привітання без IG, кнопки нема, роут відповідає 503).

## Хвиля G — Фото-інтелект: завантаження будь-яких фото + AI-аналіз (~1.5–2 дні)

> Вимога власника: клієнт може завантажити БУДЬ-ЯКІ фото (лого, фото робіт, фото/скріншоти відгуків, меню, інтерʼєр…) — прямо в чаті; AI читає кожне фото, аналізує придатність і сам розкладає по місцях.

- [x] **G1. Аплоад у чаті.** _Нотатка: скріпка ділить прапорець `loading` з відправкою (взаємовиключні); канвас-обробка виділена в `lib/media/client-image.ts` — СВІДОМИЙ дублікат логіки PhotoField (той тримає незакомічені зміни паралельної сесії; дедуплікація після її мержу)._ Кнопка-скріпка в інпуті онбординг-чату (не лише окремий media-крок): фото йдуть через існуючий `/api/upload` (по `conversationId`), у спільний пул `facts_state.media`.
- [x] **G2. Аналіз кожного фото.** _Нотатка: модуль `lib/media/analyze-photo.ts` + generic server action `analyzePhotoAction(url)` (без привʼязки до conversation/host — переюзабельний для редактора і E5); класи `logo|work|interior|menu|review|person|irrelevant` — спільний enum `PHOTO_KINDS` у media.ts; alt і OCR — суворі anti-invention правила в tool-схемі; sharp-варнінги їдуть у тому ж результаті._ Новий server action `analyzePhotoAction(url)`:
  - технічна якість — існуючий sharp-шар `analyzeImage` (розмиття/яскравість/роздільність);
  - **vision-класифікація** (Claude, одне зображення на виклик): `kind: logo | робота | відгук-скріншот | меню/прайс | інтерʼєр | людина | нерелевантне`, вердикт придатності + коротке «чому», пропозиція alt-тексту;
  - rate limit `img_analyze` (новий LimitName), вартість vision-виклику — копійки.
- [x] **G3. Маршрутизація за класом:** _Нотатки: (1) клас `person` (людина/команда) маршрутизується у фото-пул нарівні з work/interior/menu — реальні фото людей бізнесу легітимний контент (тактичне рішення, у плані клас не мав маршруту); (2) `photoMeta` (url→kind/alt) їде в `facts_state.media`, vision-alt виграють у детермінованих alt у hero/галереї генерації (гачок D3); (3) відгук-скріншот НЕ потрапляє у фото-пул — лише OCR-кандидат з карткою підтвердження, автор редагований, порожній → «Клієнт»._
  - лого → `media.logoUrl`;
  - робота/інтерʼєр/меню → `media.photos` (**підняти кап ≤3 → ≤8**, `media.ts:38`; галерея вміє);
  - **скріншот відгуку → OCR текст відгуку** (та сама vision-модель) → кандидат у `facts.testimonials` — ОБОВʼЯЗКОВО з підтвердженням користувача перед збереженням (інваріант «не вигадуємо фактів»: показати витягнутий текст — «зберегти такий відгук?»);
  - непридатне (розмите/темне/чуже) → мʼяка відмова з причиною і проханням іншого фото.
- [x] **G4. Агент бачить фото.** _Нотатка: блок «ВЖЕ ЗАВАНТАЖЕНО» в системному промпті (лого + к-сть фото по класах з photoMeta) через обидва входи (SSE route + fallback action); відгуки в інвентар не входять (вони не у фото-пулі) — підтверджені видно у facts JSON. E2E: агент згадав збережений відгук («один вже є від Оксани») і не перепитував про лого/фото._ У промпт онбордингу — список завантажених фото з класифікацією («є лого, 3 фото робіт, 1 відгук») → агент коментує природно («бачу фото робіт — піде в галерею») і не перепитує те, що вже видно.
- [x] **G5. Fail-open:** аналіз упав → фото просто лягає в пул як «робота» без класу; блокувати завантаження не можна. _Нотатка: fail-open на кожному шарі — analyzePhoto повертає null (нема ключа/таймаут/未розпарсилось), analyzePhotoAction {ok:false} (в т.ч. rate limit img_analyze), клієнт кладе фото в пул без meta._
- [x] **G6.** Той самий `analyzePhotoAction` переюзати в редакторі (PhotoField) і для Apify-імпорту (E5): всі вхідні фото проходять один інтелект-шар. _Нотатка (частково відкладено): action зроблений generic (лише url, без привʼязки до conversation/host) — готовий для E5 і редактора. Вписування в PhotoField НЕ зроблено: файл тримає незакомічені зміни паралельної сесії (заборонено чіпати правилами хвилі). Гачок на після-мерж: викликати analyzePhotoAction після аплоаду в PhotoField + дедуплікувати client-image.ts._

> **Нотатка хвилі G (2026-07-19, verification + adversarial review):** гілка wave-G від wave-D (стек H1-H5 → A → B → C → D → G; master досі лише доки). Живий E2E (Playwright, реальні API-виклики): розмова → скріпка → лого класифіковане і стало logoUrl («Бачу лого — поставив у шапку»), «фото меню» → пул з kind=menu і чесним vision-alt («Меню кав'ярні з шістьма позиціями та цінами в гривнях: еспресо…»), скріншот відгуку → OCR ДОСЛІВНО + картка підтвердження (автор «Оксана Коваль» з скріншота) → testimonial у facts; наступний хід агента згадує відгук і НЕ перепитує про лого/фото (G4 працює). Смоук генерації через розширений `/api/dev/generate` (тепер приймає media, dev-only): vision-alt відрендерився на hero (`wave-g-smoke.lvh.me`). Codex-рев'ю: 2 must-fix підтверджено і виправлено — (1) lost-update гонка saveMediaAction||saveTurn при аплоаді в чаті → аплоад-флоу персистить ОДНИМ saveTurn-записом (saveTurn прийняв опційний валідований media; saveMediaAction лишився тільки для media-кроку); (2) порожній OCR-автор мовчки ставав «Клієнт» → поле тепер видимо префілиться «Клієнт» (зберігається рівно те, що показано). Додатковий власний фікс поза кодекс-списком: saveTurn всередині setMessages-updater (сайд-ефект у рендер-фазі, React-помилка в консолі) → винесено в хендлер. Технічні борги/нотатки: (а) PhotoField-вписування G6 відкладено (файл у паралельної сесії), гачок: analyzePhotoAction після аплоаду + дедуп client-image.ts; (б) смоук-тенанти в базі поповнились `wave-g-smoke` і два demo-placeholder'и E2E-розмов — видалити пачкою з попередніми; (в) відгук-скріншоти лишаються в бакеті, у фото-пул не потрапляють (оригінали не знищуємо — норм); (г) саморобний E2E-урок: чекати `input:not([disabled])`, а не зникнення typing-індикатора.

> **Нотатка хвилі G, follow-up UX (2026-07-20, attach-then-send):** за фідбеком власника скріпка більше НЕ вантажить фото миттєво — файли чіпляються до композера (кілька одразу, до 8, локальні превʼю з ✕), і вся обробка йде лише по «Надіслати»: паралельний аплоад+vision, роутинг у ОДНЕ зведене повідомлення асистента, черга карток підтвердження відгуків (по одній), потім (якщо був текст) звичайний хід агента. `ChatMsg` отримав `attachments` (storage-URL; модель бачить лише маркер «[надіслано N фото]», ніколи URL — §4.8); saveTurn фільтрує їх через isStorageUrl; мініатюри в баблі переживають reload. Дизайн затверджений власником (спека: `docs/superpowers/specs/2026-07-20-chat-photo-attachments-design.md`). Ревʼю плану + ревʼю диффу (обидва — субагенти): виправлено (1) порядок startConversation ДО оптимістичного бабла, (2) batch-saveTurn тепер awaited — інакше міг приземлитись після запису ходу агента і стерти його відповідь, (3) revoke blob-URL на unmount. Знахідка E2E: `crypto.randomUUID` недоступний на `http://app.lvh.me` (не secure context) → локальний лічильник id. Живий E2E ×2 (до і після фіксів) зелений: батч лого+меню+відгук з текстом → 3 превʼю → зведення → картка відгуку → відповідь агента; photo-only send без ходу агента; після reload 4 storage-мініатюри в історії. Побічне: +2 demo-розмови від прогонів E2E — у ту саму чистку.

## Хвиля H — Якість генерації: розбір кейсу «Салон Діва» (~2–2.5 дні)

> Живий згенерований сайт salon-krasy-diva.wizz-app.net розібраний трьома шляхами: скріншоти (Playwright), база (brand/facts/blocks), код шаблону. Частина знахідок — системні, не про один шаблон.

**СИСТЕМНІ (вплинуть на всі сайти):**

- [x] **H1. Лого: один повний конвеєр (без проміжних етапів — рішення власника).** _Нотатки: (1) палітри шаблонів для vision-промпту колокуються в `lib/media/logo-adapt.ts` (NAV_SURFACES), не в registry — метадані внутрішнього промпту; (2) адаптація тригериться на publish, на завантаженні лого в редакторі та на first-time template pin у regenerate; (3) тумблер — самодостатній `LogoDisplayPanel`, вставка в EditorShell мінімальна (паралельна сесія тримає там незакомічені зміни)._ Сьогодні лого не рендериться на template-сайтах взагалі (`TemplateBrand` не має поля; клієнт «Діви» лого завантажив — воно лежить у базі мертвим вантажем). Будуємо одразу кінцеву механіку:
  - `TemplateBrand` + `logoUrl`; `buildTemplateBrand` прокидає (публічний рендер + frame); Nav УСІХ шаблонів рендерить лого;
  - при завантаженні/генерації: vision-аналіз лого (кольори, фон, стиль) проти палітри обраного шаблону → якщо бʼється (білий квадрат на темному тощо) — **nano-banana** (gemini-2.5-flash-image, вже в стеку, ~1.5 грн) адаптує: прибрати фон/підігнати кольори, промпт-правило «адаптуй, НЕ перемальовуй»;
  - адаптоване → `brand.logoAdaptedUrl` поруч з оригіналом (оригінал недоторканний); сайт показує адаптоване;
  - «Лого»-шит редактора: тумблер «Оригінал / Адаптоване» — інструмент ревізії власника (він свідомо приймає, що генерація інколи не ок, і передивиться);
  - fail-open: генерація впала або лого і так пасує → оригінал. Ризик дрейфу (журнал #46) прийнятий власником.
- [x] **H2. Галерея дублює hero-фото.** У базі gallery містить ВСІ 3 фото включно з тим, що вже в hero. Фікс у grounding (`groundImages`): фото, використане в hero, виключати з галереї (галерея = фото 2..N; якщо лишається <2 — галерею не створювати).
- [x] **H3. Вигадані/биті CTA-лінки.** Hero CTA «Записатись в Instagram» веде на голий `https://instagram.com` (модель вигадала). Фікс: grounding-прохід по ВСІХ href у props — дозволені лише `#якорі`, `tel:`, наші Storage-URL і соцлінки З ФАКТІВ; решта → `#lead_form`. (Зчеплено з E: коли instagram у фактах — лінк на профіль легальний.)
- [x] **H4. Ціни не зібрані.** У facts 8 послуг — жодної ціни (корінь скарги «у формі нема цін»: їх нема в даних). Фікс у промпті онбордингу (частина A): якщо послуги без цін — ОДНЕ просте питання «назвіть орієнтовні ціни хоча б для головних послуг; можна "від"»; генерація не малює порожні прайс-колонки.
- [x] **H5. Scroll-reveal ховає контент.** _Нотатка: закрито scroll-reveal (ferri Reveal, salon ScrollReveal+SalonStats, studio inline whileInView → gated shared reveal). Load-анімації героїв/навів (initial+animate, ~19 файлів) лишаються framer-hidden до гідрації — вони самі програються при завантаженні JS; окремий прохід, якщо власник захоче 100% no-JS purity._ Секції стартують невидимими (анімація появи) — на fullPage-скріншоті пів сторінки порожні. Перевірити SSR-вивід: якщо контент opacity-0 до JS — ризик для SEO-ботів і повільних пристроїв; reveal має бути прогресивним посиленням (видимий без JS).
- [ ] **H6. QA-конвеєр генерації (загальне покращення, яке просив власник) — одразу з авто-фіксом.** Після генерації автоматично: Playwright-скріншот сайту (desktop+mobile) → vision-перевірка моделлю (чек-лист: лого є? фото не дублюються? кнопки ведуть кудись? мова 100% українська? верстка не поїхала?) → безпечні знахідки (тексти/лінки/дублі/приховати зайве) агент виправляє сам через існуючі editor-tools у чернетці, решта — в лог адмінки. Страхує всі майбутні шаблони.(ТУТ ПОКИ НЕ ІМПДЕМЕНТУЄМО І НЕ СПІШЩИМО З ЦИМ )

> **Нотатка хвилі H1–H5 (2026-07-17, adversarial review):** codex-рев'ю дало 12 знахідок; виправлено: мертві `#lead_form`-якорі на класичних сайтах + валідація якорів проти реальних секцій (пост-прохід у `assemble`), lost-update гонка в `setLogoAction` (адаптація ДО запису + свіже перечитування brand), відсутній `revalidateTenant` при brandPatch у regenerate, валідація mime/розміру виходу gemini, Content-Length guard, `max-w` на лого в усіх Nav, салонний `ScrollReveal` перебіндює observer по `armed` (framer `useInView` дивився на відʼєднаний вузол). Прийнято свідомо: remount-at-hydration у gated reveal (ціна анімації; вікно — момент гідрації below-fold елементів), `generateAndPublish(publish=false)` як і раніше переписує unversioned brand без purge (dev-шлях, до окремого рефакторингу). Побічне: draft-тенант `wave-smoke.lvh.me` лишився в базі після smoke-тесту (SQL-тул read-only) — можна видалити.

**ШАБЛОН SALON (і аудит решти портованих):**

- [ ] **H7. Англійські залишки.** «View More Work» (`SalonGallery.tsx:96`, `SalonGalleryAlt.tsx:78`), eyebrow «PORTFOLIO» — аудит `components/templates/**` на hardcoded EN-строки, замінити на українські (або props з UA-дефолтами).
- [ ] **H8. Кнопка «View More Work» недоречна** при 3–4 фото: рендерити лише якщо фото > 6 (і з UA-текстом), інакше прибрати.
- [ ] **H9. Клікабельні фото без лінків.** Тайли галереї виглядають клікабельними, але нікуди не ведуть: або лайтбокс (збільшення по кліку), або прибрати hover/cursor-poiner семантику.
- [ ] **H10. Hero-верстка salon (variant editorial):** на десктопі фото зверху, текст-картка знизу зі зсувом — переверстати (текст ліворуч, фото праворуч або повноекранний layout).
- [ ] **H11. Навігація salon:** довгі мітки («Як відбувається візит», «Питання та відповіді») ламаються на два рядки + завеликий кегль. Додати короткі `navLabel` у section map шаблону («Візит», «Питання») і зменшити типографіку Nav.

---

## Порядок виконання і оцінка

| Хвиля     | Що                                                      | Оцінка             |
| --------- | ------------------------------------------------------- | ------------------ |
| A         | Вільний збір + чат-підтвердження замість форми (A5, A6) | 1–1.5 дня          |
| B         | Шаблони в чаті + передумування                          | 1 день             |
| C         | Повторні секції                                         | 1 день             |
| D         | SEO-генерація                                           | 1–1.5 дня          |
| G         | Фото-інтелект (аплоад у чаті + vision-аналіз)           | 1.5–2 дні          |
| E         | Instagram повний (хендл → Apify → сайт)                 | 2–2.5 дні          |
| H         | Якість генерації (кейс «Діва» + QA-конвеєр з авто-фіксом) | 2.5–3 дні        |
| **Разом** |                                                         | **~10–12.5 днів**  |

Рекомендований порядок: **H1–H5 (гарячі баги живих сайтів) → A+B (один коміт) → C → D → G → E (переюзає G-аналіз) → H6–H11**. C незалежна; D після C (SEO-правила враховують повтори).

## Ризики

1. **C:** повторена секція з ТИМ САМИМ variant виглядає як багофний дубль → промпт-правило «різні variants» + кап 2. Якщо шаблон має один variant секції — не повторювати (перевірка в assemble).
2. **A:** «вільність» може розтягнути розмову → правило темпу (A3) і ready-gate лишаються запобіжниками; латентність ходу вже покрита thinking.
3. **D3-схема, E7-футери, H7–H11** — зміни словника блоків/шаблонів = зона template-агента, координувати.
4. **E:** стабільність Apify-актора → fail-open всюди; згода користувача — явна, фото тільки його власні.
5. **H1/H6 (генеративні лого-адаптації й авто-фікси):** власник свідомо приймає, що результат інколи не ок — тумблер «Оригінал/Адаптоване» і undo агента = інструменти швидкої ревізії.
