Minimal lettermark logo of the numeral three, where the numeral is constructed from three identical stacked horizontal rounded bars like tidy rows of a webpage, each bar's right end curling into the numeral's two open bowls so the stack itself reads as the digit. Bold uniform stroke weight, generous rounded terminals, geometric grid construction with soft corners. Warm confident modern tech register, friendly not corporate. Two colors only: the numeral in the deep primary color, one small circular accent dot floating at the upper right of the numeral like a notification, in the accent color; solid flat fills, no gradients, plain background. Single centered mark with even clear space on all sides. Flat vector design, clean lines, no shadows, no texture, no text. Clean editable vector paths, SVG-friendly, minimal anchor points.
